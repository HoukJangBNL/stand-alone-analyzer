# Project Status

> **Living document.** PM이 작업 종료 시점마다 갱신.
> 사용자(오너)가 "지금 어디까지 와 있나" 30초 안에 파악할 수 있어야 함.
> 디테일 디자인·DDL·운영 절차는 별도 문서로 링크 — 여기에는 **상태**만.

**Last updated**: 2026-06-09 (저녁) — **GPU Dispatcher 1-click acceptance §32 (post-vendor-revert) — USERDATA_BLOCKED_3 ❌ vendor gitlink revert(`6090d9c`)가 §31의 차단점에 직접 효과 없음 — 같은 라인, 같은 에러 재현**. owner directive("vendor gitlink을 baked SHA로 되돌리면 `git submodule update --init --force`가 no-op")는 git-submodule-update 의미를 잘못 모델링한 것. 라이브 인스턴스에서 SSM 검증: `git ls-tree HEAD vendor/QPress-SAM-Flake` = `2c69ebd` AND baked submodule HEAD = `2c69ebd` (정확히 일치) → 그럼에도 `git submodule update --init --recursive --force`는 unconditional remote fetch 시도 → 인증 실패. **이유**: `--init`은 gitlink-vs-local-SHA 비교 안 함, 매 호출마다 `submodule sync` + remote fetch 실행 후 working tree reset (이게 `--init`의 정의된 행동). **검증된 것 ✅**: T7c 다시 정상 (3번째 연속), 메인 레포 fetch 정상 (`e2a2ede..6090d9c`), `gpu_launching` SSE 다시 +3.17s, spot capacity 안정 (us-east-2a 첫 시도 즉시 할당), §31 spot-cancel 청소 효과로 quota-lag false-start 재발 없음. **권고 T7e (= §31 T7d Option A 재진술)**: user-data 라인 185-186을 `git ls-tree HEAD vendor/QPress-SAM-Flake` vs `git rev-parse HEAD` 비교로 교체 — 일치하면 fetch skip, 불일치하면 "AMI re-bake required" 명시 exit. AMI 재베이크 불요, LT v30 publish만. 코드: `docs/sam-ops.md §32.3`. **이번 비용 ~$0.55** (g6e.48xlarge spot 5.5분; PM 수동 terminate). **여전히 미검증**: step 5-8 (venv/M3/worker.service/idle-timer) 새 AMI에서 새 하니스로 첫 실행 안 됨 — T7e 후 다음 layer 노출 가능성 명시. **§27+§28+§29+§30+§31+§32 누계 ~$3.88. $100 cap 잔여 ~$75.58.** 상세: [`docs/sam-ops.md §32`](sam-ops.md#32-1-click-sam-dispatch-acceptance--2026-06-09-post-vendor-revert-userdata_blocked_3).

**Previous update (T6 fourth retry)**: 2026-06-09 (오후) — **GPU Dispatcher 1-click acceptance §31 (post-T7c) — USERDATA_BLOCKED_2 ✅ T7c `git config --system` fix 풀 검증 (cloud-init 환경에서 정확히 의도대로 동작), 그러나 다음 라인(submodule update)이 노출한 새 차단**. T7c(`d929ce3`, LT v28 default published) 검증 위해 §30 하니스 재실행. **검증된 것 ✅**: (1) T7c가 cloud-init의 빈 env root context에서 정확히 동작 — `git config --system` 성공, `[safe.directory] *` `/etc/gitconfig`에 기록, 메인 레포 `git fetch origin` 정상 (baked `e2a2ede` → `d929ce3` advance), `$HOME not set` 사라짐, `dubious ownership` 사라짐 / (2) `gpu_launching` SSE 다시 +3.25s 만에 fire (§30과 동일 timing) / (3) §28.3 quota-lag 패턴 재현 + 30초 내 해결 (수동 `cancel-spot-instance-requests sir-iv87j31j` 후 재시도 즉시 성공). **새 차단 모드(§31이 처음 노출 — §28-§30이 모두 라인 163 `--global`에서 abort했어서 라인 180까지 도달 못 했었음)**: user-data 라인 180 `git submodule update --init --recursive --force vendor/QPress-SAM-Flake`이 런타임 GH PAT 부재로 `fatal: could not read Username for 'https://github.com'` → cloud-init abort. submodule(`HoukJangBNL/QPress-SAM-Flake`)이 private이고 PAT은 베이크 시점에만 노출됨(#225/#228 디자인). `--force`가 베이크된 SHA가 gitlink와 이미 일치(`2c69ebd`, `git log e2a2ede..d929ce3 -- vendor/QPress-SAM-Flake` 빈 결과 — T7c push에 submodule 변경 0건)함에도 불구하고 강제 재페치 시도 → 인증 실패. **권고 T7d (Option A, AMI 재베이크 불요)**: 라인 180을 baked SHA vs gitlink 비교로 교체 — 일치하면 fetch skip(99% 케이스, 베이크가 source of truth), 불일치하면 명시적으로 "AMI re-bake required" 메시지로 exit. 코드: §31.3 참조. LT v29 publish만 필요. (Option B = 런타임 PAT 주입은 secret-at-runtime 부담으로 비추천.) **이번 비용 ~$0.62** (g6e.48xlarge spot ~6.2분 × $5.96/hr; PM이 cloud-init wedge 식별 후 수동 terminate; idle-shutdown.timer 미설치라 자동 종료 못 했을 것). **추가 quota 정리**: 두 spot request(`sir-iv87j31j` §30 잔존, `sir-rftqjw5g` §31b) 모두 명시적 cancel — 다음 attempt에서 §31.1 false start 재발 방지. **여전히 미검증**: `gpu_ready`/per-image progress/`done`/`idle-shutdown.timer` (worker.service가 step 5/6/7 도달 못 해서 미설치). **§27 + §28 + §29 + §30 + §31 누적 dispatcher 비용 ~$3.33. $100 cap 잔여 ~$76.13.** **부수 권고 T9 (acceptance scope 밖, 별도 플랜)**: `_terminate_worker`가 spot 인스턴스 종료 시 spot request도 같이 cancel하도록 wiring — §28.3 + §31.1 quota-lag 재발 방지. 상세: [`docs/sam-ops.md §31`](sam-ops.md#31-1-click-sam-dispatch-acceptance--2026-06-09-post-t7c-userdata_blocked_2).

**Previous update (T6 third retry)**: 2026-06-09 — **GPU Dispatcher 1-click acceptance §30 (post-T7b, capacity restored) — USERDATA_BLOCKED.** PM live-launch probe로 us-east-2 `g6e.48xlarge` spot capacity 회복 확인 후 §29 하니스 1회 재실행. **검증 ✅**: (1) capacity 복귀 — POST 후 3초 만에 spot 인스턴스 `i-0453e6a88fa017783`(us-east-2a) `running`, on-demand 폴백 불필요, (2) **`gpu_launching` SSE 첫 end-to-end 관측** — `_launch_one` → `bridge.emit_gpu_launching` 성공 경로 와이어 검증, instance_id 캐리, (3) SSE keepalive 12분+ 안정. **새 차단(§30이 처음 노출)**: T7b의 user-data 라인 163 `git config --global --add safe.directory '*'`가 cloud-init `scripts_user`(root, 빈 env, `$HOME` 미설정)에서 `fatal: $HOME not set` exit 128 → `set -euo pipefail`로 즉시 abort → step 5/6/7(venv/M3/worker.service) 미실행 → 워커가 부팅 안 됨. T7b 의도(dubious-ownership 회피)는 맞았으나 cloud-init context를 못 봤음. **권고 T7c (2-line fix, AMI 재베이크 불요)**: `git config --global` → `git config --system`(preferred, `/etc/gitconfig`에 써서 HOME 의존 제거) 또는 `HOME=/root git config --global` 명시; LT v28 publish만 필요(AMI `ami-0b7ec5ff47a1eff11` 그대로). **여전히 미검증**: `gpu_ready`/per-image progress/`done`/`idle-shutdown.timer` (worker.service가 안 깔려서). **이번 비용 ~$1.19** (g6e.48xlarge spot ~12분, PM이 wedge 식별 후 수동 terminate). **§27 + §28 + §29 + §30 누적 dispatcher 비용 ~$2.71. $100 cap 잔여 ~$76.75.** 상세: [`docs/sam-ops.md §30`](sam-ops.md#30-1-click-sam-dispatch-acceptance--2026-06-09-post-t7b-capacity-restored-userdata_blocked).

**Previous update (T6 second attempt)**: 2026-06-08 (저녁) — **§28 (post-T7) — PARTIAL_BLOCKED (T7 spot→on-demand fallback ✅ 검증 + `gpu_launching` SSE 첫 관측 ✅, 다만 SAM 성공 경로는 latent 버그 2개에 가로막힘).** §27의 권고였던 `_launch_one` on-demand 폴백을 T7(`edf8bd3`)로 ship 후 §27 하니스 재실행. 결과: T7 폴백 wire-path 정상(spot drought→on-demand 시도→두 콜 모두 `InsufficientInstanceCapacity`→`GpuCapacityUnavailable` SSE 에러 envelope, message 문자열로 wire 검증) / capacity 복귀 후 spot 인스턴스 `i-0dd944abe7d9e432a` 정상 launch + **`gpu_launching` SSE 첫 관측(instance_id 캐리)**. **그러나** 그 시점에 latent 버그 2개 surfaces: (1) **API-side `procrastinate.AppNotOpen` at `defer_async`** — 본 사이클에 T8로 패치(`api/main.py` lifespan에 `app.open_async()` 추가); (2) **AMI bake `git fatal: detected dubious ownership`** — 사전 베이크된 `/opt/sam/stand-alone-analyzer`가 `ubuntu` 소유인데 cloud-init은 `root`로 돌아 user-data `[4/8] clone repo` 첫 줄에서 abort, **`worker.service` 시작 안 됨**(`systemctl is-active worker.service`=`inactive` SSM 확인). 따라서 `gpu_ready` / per-image progress / `done` / 10-min idle terminate 여전히 미검증. **이번 비용 ~$1.52** (spot 2대 합 ~14.5분 wall, $5 cap 대비 충분). **누계: $20.54 + $1.52 = $22.06, $100 cap 잔여 ~$77.94.** **권고 T9 (다음 사이클, owner 결정)**: AMI bake에 `chown -R root:root /opt/sam/stand-alone-analyzer` 또는 user-data 첫 줄 `git config --global --add safe.directory '*'` 추가 + bake CI에 SSM smoke check (`worker.service is-active == active` ≤ 5min). 상세: [`docs/sam-ops.md §28`](sam-ops.md#28-1-click-sam-dispatch-acceptance--2026-06-08-post-t7-partial_blocked).

**Previous update (T6 first attempt)**: 2026-06-08 (오후) — §27 CAPACITY_BLOCKED. spot 4회 시도 모두 `InsufficientInstanceCapacity`, EC2 한 대도 안 뜸, 비용 $0. 권고 follow-up "_launch_one on-demand 폴백" 즉시 채택 → 위 §28에서 재실행. 상세: [`docs/sam-ops.md §27`](sam-ops.md#27-1-click-sam-dispatch-acceptance--2026-06-08-capacity_blocked).

**Previous update (Task 13 attempt 7)**: 2026-05-31 — manual SSH/SSM, orchestrator 우회 BLOCKED. AMI v25 SAM 컴퓨트 레이어 2개 latent 버그(merged_m3 weight format, cuDNN sublib mismatch). **총 비용 $4.74**, **측정 0회**. 상세: [`docs/sam-ops.md §26`](sam-ops.md#26-8-gpu-100-image-manual-measurement-run-2026-05-31-229-follow-up--blocked).

**Previous update (Task 13 attempt 5)**: 2026-05-30 — phase 7 defer가 `sleep 10` 단일 wait로 SSM 명령이 `InProgress` 상태에서 stdout 비었음 회수, `JOB_ID` 미설정으로 exit 4. PM이 `d87916c`로 phase 7 폴링 루프(180s cap, Status 분기) 추가. 상세: [`docs/sam-ops.md §24`](sam-ops.md#24-8-gpu-100-image-measurement-run-2026-05-30-229-follow-up-t13-attempt-5--blocked-at-phase-7-defer-orchestrator-polling-race).

**Previous update (Task 13 attempt 4)**: 2026-05-29 — userdata step 2가 DLAMI 드라이버를 in-place 교체해 `nvidia-smi`가 2/8만 enumerate. PM이 `5c2adaa`로 "DLAMI 드라이버 작동 시 cuda-drivers skip, toolkit만 설치" 분기 추가. 상세: [`docs/sam-ops.md §23`](sam-ops.md#23-8-gpu-100-image-measurement-run-2026-05-29-229-follow-up-t13-attempt-4--blocked-at-pre-flight-only-2-of-8-gpus-visible).

**Previous update (Task 13 attempt 3)**: 2026-05-29 — user-data `[4/8]` git submodule 실패. PM이 `2b8d568`로 `git reset --hard` + `git submodule sync --recursive` 추가. 상세: [`docs/sam-ops.md §22`](sam-ops.md#22-8-gpu-100-image-measurement-run-2026-05-29-229-follow-up-t13-attempt-3--blocked-at-user-data-git-submodule).

**Previous update**: 2026-05-27 (**Phase 4 closure** — P4.0 ✅ / P1.5b ✅ / P4.1 ✅ / P4.2 ✅ / P4.3 ✅ / P4.4 ✅ / P4.5 ✅ + IAM extras inline policy `qpress-sam-phase4-extras` (Option B, SSM only) ✅ + SSM `/qpress-sam/db_password` SecureString ✅ + `/qpress-sam/db_username` (`houk`, RDS master) ✅. **#190 Real-GPU SAM e2e는 backlog 유지** — owner GPU 작업 시작 시점에 spot launch + 1-image inference + DB INSERT 검증, Phase 4 closure에 묶지 않음. **Phase 5 ✅** (P5.1~P5.5 closed). 보안 후속(backlog): `qpress_worker` dedicated app user 생성 후 SSM rotate — 현재는 master cred 사용 중.)
**Current branch**: `feat/migration-cutover` (**1차 목표 도달 — Upload + ScanTable + Delete + Phase E real-data 풀세트 검증 완료**. 진행 마일스톤: W3.5 / W2.4 / W8.1 / W6 (auth+ACL+usage) / W5 전체(A/infra/B1/B2/C) / admin_usage 격리 fix / W10 A→E 전부 / #64 fix / Robustness Phase A→D 전부 / **Phase E real-data 검증(E1-E5 + E6 풀세트 3648 PNG/9GB PASS)** / Hotfix #10 subfolder toggle / W11 scan-guards 전체(T1-T9) / W12 ScanTable + Delete 전체(T1-T12 + IAM fix) / CORS AWS apply 라이브 정합 확인. **다음 마일스톤: Compute Pipeline** (param form + GPU SAM + workflow runner — brainstorm 8개 결정 잠금, 기존 `2026-05-25-segmentation-web-integration.md` 플랜에 Phase 0(decision lock) + Phase 5(Pipeline UX) 흡수, W7 sketch superseded). 잔여: W6.1 Cognito 부트스트랩(AWS 승인 대기) / W8 Part 2 SSM / W9 reg_covar.)
**Schema version**: `v7.1` (alembic `0003_w5a_materials_uploads` 헤드 — materials 테이블 + scans/images 강화. 로컬 saa_test 적용 완료. RDS는 W6.1 Cognito 승인 후 함께)

---

## 1. Big picture

마이그레이션 진행 중: **Streamlit standalone analyzer → React + FastAPI + PostgreSQL** (RDS).
GUI는 React/web, API는 FastAPI, 데이터는 RDS, 무거운 계산은 GPU 워커(g6e.* spot 예정).

---

## 2. 최근 마일스톤 (완료)

| 시점 | 내용 |
|---|---|
| 2026-05 | DB 스키마 v6 확정 ([`db-schema-v6.md`](db-schema-v6.md)) |
| 2026-05 | RDS `qpressdb` 클러스터 빈 상태 확인, `qpress` DB 생성 |
| 2026-05 | RDS 마스터 비번 → AWS Secrets Manager 관리로 전환 (`--manage-master-user-password`) |
| 2026-05 | Bastion EC2 (`i-063165d449976b2e4`, t4g.nano) + SG 셋업, SSH 터널 검증 |
| 2026-05 | Alembic 셋업 + `0001_initial_v6` RDS 적용 (15 테이블 + 3 ENUM + system user 시드) |
| 2026-05 | SQLAlchemy 2.x ORM 모델 작성 (그룹별 7개 파일) |
| 2026-05 | 운영 런북 ([`db-ops.md`](db-ops.md)) 작성 |
| 2026-05 | PM 역할 정의 (`CLAUDE.md`) — 에이전트 오케스트레이션 모델 도입 |
| 2026-05-21 | 영속 에이전트 7개 정의 (`.claude/agents/`): db-specialist, api-developer, frontend-architect, algo-engineer, devops-engineer, researcher, code-reviewer |
| 2026-05-21 | **W4.2** ORM `Computed` 수정 + manifest `step_status` reconciliation 헬퍼 — `Analysis.status`를 `Computed(..., persisted=True)`로 선언(GENERATED 컬럼 메타데이터 정합), `src/flake_analysis/db/reconcile.py` 추가. DB가 step status의 source of truth. → [`docs/superpowers/plans/2026-05-21-W4.2-db-orm.md`](superpowers/plans/2026-05-21-W4.2-db-orm.md) |
| 2026-05-21 | **W4.4** 클러스터링 spec 변경 + auto-opt — `reg_covar`를 엔진/래퍼/매니페스트에 노출(디폴트 `10.0`), `auto_tune_reg_covar` 드라이버(blob-recall × Mahalanobis margin), W4.3 invariant 강화(시드 블롭 100% 캡쳐), parity golden 재생성(clustering 블록 한정). 342 tests pass. → [`docs/superpowers/plans/2026-05-21-W4.4-clustering-spec.md`](superpowers/plans/2026-05-21-W4.4-clustering-spec.md) |

---

## 3. 진행 중 / 다음 작업

### 3.1 다음 한 발
**Compute Pipeline — Phase 5 backend P5.2 완료(2026-05-27)** — `POST /run/pipeline` orchestrator(thumbnails 독립 → background → sam → {stats || prox} 병렬) + multi-step SSE 어휘(`step_started`/`step_progress`/`step_completed`/`pipeline_done`/`pipeline_error`) + Background cascade(파라미터 dirty 시 sam/stats/prox 자동 재실행 + Domain/Flake DB row 삭제) 라이브. commit `b687cac`. 신규 파일: `api/routes/run_pipeline.py`(359), `api/services/analyses.py`(`get_or_create_default_analysis` + `DEFAULT_AMG_PARAMS`), `api/services/cascade.py`(`apply_background_cascade_if_needed`), `tests/api/test_run_pipeline_sse.py`(3 PG-marked tests). `api/sse.py`에 `PipelineProgressBridge` 추가. P5.1 owner sub-decision Option B(영속화 SKIP, client-side default) — scope creep 회피. **다음 한 발 — P3.2 SamRunPanel(frontend-architect, in flight)** → P5.3(usePipelineProgress + PipelineParamsForm + PipelineTimeline) → P5.4(ComputeTab 재설계 + cascade confirm dialog) → P5.5(acceptance gate) → Phase 4(owner 게이트).

**Phase 2 산출물**:
- **P2.1** `src/flake_analysis/state/paths.py` — `sam` step 등록 (PIPELINE_STEPS 8 entries, SUBDIRS["sam"]="07_sam", ARTIFACTS["sam"]=["per_image_results.json"]). commit `241fe5d`.
- **P2.2** `src/flake_analysis/core/pipeline/sam.py` — vendor `run_amg_v2_inference` 어댑터(`_vendor_infer` lazy import + `(payload: dict)` → `(pct, msg)` shim + summary persist). commit `1720f6e`.
- **P2.3** `src/flake_analysis/pipeline/sam.py` — `run_sam_step` wrapper. commit `509dbe8`.
- **P2.4** `src/flake_analysis/api/services/runs.py` — `record_run_start` / `record_run_end` + PG-marked test. commit `c9f22a7`. **PipelineStatus 값은 `pending|running|completed|failed`** (NOT `succeeded`).
- **P2.6** 4개 per-step SSE 라우트(background/sam/domain_stats/domain_proximity)에 runs INSERT/UPDATE wiring + `get_session_for_background()` 헬퍼 추가. commit `e91f892`.
- **P2.7** `POST /run/sam` 신규 라우트 + `SamParams`/`SamSummary` 스키마 + 4 PG-marked tests. commit `97ed40f`.
- **P2.8 + P2.8b** Phase 2 acceptance gate 실행 + stale `tests/test_state_paths.py::test_pipeline_steps_count` (7→8) fix. commit `72ec1a2`.

**Phase 1 완료(2026-05-26)** — segmentation-web-integration 플랜의 **Phase 1(SAM 포트 + 머지 CLI)** 마감. fork PR #1 rebase-merge → main 레포 submodule pointer `cbf5237 → 68aa25e`(`e7adcd3`).
- **P1.1** `vendor/QPress-SAM-Flake` git submodule 임베드(HoukJangBNL/QPress-SAM-Flake)
- **P1.2** `run_amg_v2_inference.py` — inference-only 진입점(per-image callback / IndexError surfaced / no chdir / no args.json)
- **P1.3 + P1.4** 회귀 테스트 3건(no_chdir / IndexError surfaced / no args.json lookup) PASS
- **P1.5** `scripts/merge_lora.py` + tiny CI fixture — synthetic-math TDD 4건 + smoke 1건 PASS(Option A hand-crafted Linear / Option B mock build_sam2 — 실제 SAM2 layer subset 추출은 strict load 거부로 불가)
- **P1.6** `requirements-inference.txt` 분리 — 추론 서버에서 `peft` 제거
- **P1.7** main 레포 submodule pointer bump
- **별건 후속(#162 P1.5b — Phase 4 직전까지)**: 진짜 prod ckpt 머지에는 두 가지 추가 fix 필요. (a) prod LoRA가 `image_encoder.base_model.model.trunk.*` 형태로 trunk submodule만 wrap돼 있어 현 `_strip_peft_prefix`가 못 따라감(중첩 prefix strip 또는 base_layer.weight slot 사용 필요). (b) `patch_embed.proj`는 Conv2d라 `B @ A` 대신 `(B.flatten(1) @ A.flatten(1)).reshape(target)` 필요(100 LoRA pair 중 1개). 합성 데이터 머지는 100% 정합 — Phase 4 owner 승인 후 GPU EC2에서 prod merged.pt 생성하기 직전에 처리.

**다음 한 발 — Phase 4 GO (2026-05-27 P4.0 owner sweep 완료) → P1.5b(prefix-strip + Conv2d, 코드 only) → P4.1(weights S3 upload) → P4.2(procrastinate) → P4.3(AMI/bootstrap) → P4.4(EC2 launch + spot interrupt) → P4.5(e2e + cost alarm)**. P4.0 결정 잠금(decision doc [`2026-05-27-segmentation-phase4-decisions.md`](superpowers/decisions/2026-05-27-segmentation-phase4-decisions.md)): D1 scale-to-zero on-demand spot / D2 g6e.xlarge spot / D3 S3 lazy download / D4 auto-retry once / D5 SAM-only on procrastinate / D6 $20/일 CloudWatch 알람 / D7 P1.5b 우선. AWS 비용 발생 task(P4.1 weights upload, P4.3 AMI/IAM, P4.4 EC2 launch, P4.5 alarm)는 task별 추가 owner GO 받아야 함. Phase 5 마감(2026-05-27): Compute Pipeline 백엔드 오케스트레이터 + 프론트엔드 한 폼/타임라인/cascade 도장 라이브. 실행 순서: **Phase 1 ✅ → Phase 2 ✅ → Phase 5 backend(P5.2 ✅ `b687cac`) → Phase 3 frontend(P3.1 ✅ via upload-robustness Phase C / P3.2 ✅ `25bf4b3` / P3.3 ✅ `7348058`) → Phase 5 frontend(P5.3 ✅ `2db03af` / P5.4 ✅ `f41a9d5` / **P5.5 ✅ acceptance gate PASS — 백엔드 557/3 skip + 프론트 99/380 + tsc/build green**) → Phase 4(owner 게이트)**. P5.5 게이트 로그: `.w10-acceptance.20260527T093732.log`. #180 mitigation 적용(usage_events 1 row 사전 wipe — gate idempotency 후속). brainstorm 8개 결정은 [§3.1 history note 참고](#) — 1차 목표 도달 직후 잠금: (1) 원클릭 `[▶ Run pipeline]` + 5스텝 타임라인. (2) Compute Tab 진입 시 한 폼에 thumbnails/background/sam/stats/proximity 모두. (3) 재실행 = 증분 + 분기 그래프(Thumbnails 독립, Background → SAM → {Domain Stats, Domain Proximity}). (4) GPU = 온디맨드 spot. (5) SAM 포함. (6) analysis 단위 = 스캔당 1 default analysis. (7) 파라미터 저장 = 기존 `analyses.{amg,background,proximity}_params` JSONB. (8) Background cascade = 자동 + "Will rerun: SAM, Stats, Prox" 확인 도장. **W7 sketch는 superseded** — segmentation-web-integration 플랜이 SAM 포트(Phase 1 ✅) + runs wiring(Phase 2) + frontend(Phase 3) + GPU EC2(Phase 4 owner 게이트) + Phase 0/5(decision lock + Pipeline UX) 모두 커버.
> 1차 목표(2026-05-26) — Upload + ScanTable + Delete + Phase E real-data 검증 풀세트(E6 3648 PNG / 9GB)까지 owner PASS. W12 IAM fix(`s3:DeleteObject` 추가 라이브 적용) + CORS 라이브 정합 확인 포함. 그 외 잔여(W6.1 Cognito 부트스트랩, W8 Part 2 SSM, W9 reg_covar 캘리브레이션)는 모두 외부 의존(AWS 승인 / 데이터셋) 대기. 
> 2026-05-22 — admin_usage 격리 fix 완료. 별건 백로그 3개 등록(test_scans_* s3_uri / tests/scripts drop_all footgun / test_run_*_sse hang).

### 3.2 백로그 (우선순위 순)

> 모든 백로그 항목에 대해 플랜 작성 완료. 실행 가능 여부는 각 플랜의 "Execution Handoff" 섹션을 참고.

- [x] **W3.5** 인터랙티브 클러스터링 튜너 UI — [`plan`](superpowers/plans/2026-05-21-W3.5-clustering-tuner.md) — backend slice (`reg_covar` + `auto_tune` schema/route + SSE `reg_covar_chosen`), frontend slice (log-scale slider [0.1, 10.0] + AutoTune 버튼 + ClusteringRightRail 마운트). 9 commits (`e082d90`..`ab87bf9`). 246 vitest tests pass.
- [x] **W2.4** manifest 엔드포인트 DB rewire — [`plan`](superpowers/plans/2026-05-21-W2.4-manifest-db-rewire.md) — `GET /data/manifest`가 DB의 `analyses.steps_done`를 source of truth로 오버레이 (background/sam/domain_stats/domain_proximity + 신규 top-level `status`). DB row 없으면 silent fallback, SQL error는 500 `db_unavailable`. 완료 — `2d5d7d5`.
- [ ] **W5** 업로드 플로우 — D-block 잠금(2026-05-22) → 5개 실행 가능 plan 분리 작성:
    - [x] [`W5-A 스키마`](superpowers/plans/2026-05-22-W5-A-schema.md) — `materials` 테이블 + `scans.extra_metadata` JSONB + `scans.material` NOT NULL+FK + `images.grid_ix/iy` NOT NULL+UNIQUE. alembic 0003. **완료(2026-05-22, 4 commits `1067eac`..`70064dd`, PG 21/21).**
    - [ ] [`W5-B1 materials + scan create`](superpowers/plans/2026-05-22-W5-B1-materials-and-scan-create.md) — `GET/POST /materials` + `POST /projects/{pid}/scans` (path-only routing).
    - [ ] [`W5-B2 presign + complete + finalize`](superpowers/plans/2026-05-22-W5-B2-presign-complete-finalize.md) — boto3 presigned PUT + `x-amz-checksum-sha256` enforcement + `images` row 생성 + scan finalize/usage event.
    - [ ] [`W5-C 프론트엔드`](superpowers/plans/2026-05-22-W5-C-frontend.md) — 프로젝트 페이지 모달, MaterialCombobox(auto-add), Web Crypto SHA256, 4-concurrent upload orchestrator, Playwright e2e.
    - [x] [`W5-infra AWS S3`](superpowers/plans/2026-05-22-W5-infra.md) — 버킷 + CORS(dev-only) + 라이프사이클 + IAM dev/prod + 크로스-prefix 거부 정책. **완료(2026-05-22, 6 prep commits `140b757`..`3a69369` + AWS 7 게이트 적용, dryrun 14/14 PASS).**
    - 옛 SKETCH 문서: [~~2026-05-21-W5-upload-flow.md~~](superpowers/plans/2026-05-21-W5-upload-flow.md) (Superseded).
- [x] **W6** 인증/세션 (Cognito + 4-tier 글로벌 ENUM + per-project ACL + usage events + 프론트엔드 슬라이스) — [`plan v2`](superpowers/plans/2026-05-21-W6-auth-session-v2.md) — 6 sub-plan 완료(W6.0 schema v7 / W6.1 Cognito 인프라 docs / W6.2 백엔드 의존성 / W6.3 ACL / W6.4 usage events / W6.5 프론트엔드). 28 commits (`8bf3c90`..`bd9339b`). 백엔드 비-PG 테스트 292 pass + 프론트엔드 vitest 265 pass. **W6.1 Cognito 부트스트랩 실행은 AWS 승인 대기.**
- [ ] **W10** real projects table + 1:N scans + per-scan analysis + per-scan mutex/manifest — D-block 잠금(2026-05-22) → 5개 실행 가능 plan 분리 작성:
    - [ ] [`W10-A 스키마`](superpowers/plans/2026-05-22-W10-A-schema.md) — `projects` 테이블 (TEXT PK = `gen_random_uuid()::text`) + `scans.project_id` FK ON DELETE RESTRICT + alembic `0004_w10_projects` + `scripts/db/wipe-saa-test-pre-w10.sql` (in-memory persistence only — D2/D3).
    - [x] [`W10-B active project decoupling`](superpowers/plans/2026-05-22-W10-B-active-project-decoupling.md) — `_active_project` 글로벌 제거, `SAA_ANALYSIS_FOLDER` → `SAA_ANALYSIS_ROOT` rename(legacy fallback), `acquire_scan_lock(scan_id)` asyncio.Lock per-scan mutex + `analysis_folder(root, pid, sid)` / `manifest_path(root, pid, sid)` helpers (D4/D5). **완료(2026-05-23, 4 commits `50197a4`..`8de71f9` + docs `9da1f00`).**
    - [x] [`W10-C route surface`](superpowers/plans/2026-05-22-W10-C-route-surface.md) — projects CRUD service + schemas, `routes/projects.py` rewrite, `GET /projects/{pid}/scans`, 3 parallel sub-batches migrating routers to `/projects/{pid}/scans/{sid}/...`, acceptance gate (D6 force-create UX). **완료(2026-05-23, 7 commits `9fcae04`..`02abe6f`).**
    - [x] [`W10-D 프론트엔드`](superpowers/plans/2026-05-22-W10-D-frontend.md) — `state/projectSlice.ts` `activeScanId` + cross-project clear, `api/projects.ts` CRUD-only rewrite + `listScansForProject`, `CreateProjectModal`(name+description) + `ScanPicker` 드롭다운, `App.tsx` 경로 grammar `/projects/:pid/scans/:sid/<tab>` + `ProjectScanSync`, tab 페이지 `scanId` prop + `ComputeTab` no-scan empty state, Sidebar `listProjects` + `CreateProjectModal` 통합 (legacy `CreateProjectForm` 삭제), 18 vitest 파일 3 batches. **완료(2026-05-25, 8 commits `5314879`..`044e64d`, vitest 86 files / 300 tests pass, build green)**.
    - [x] [`W10-E 테스트 + 데이터 마이그레이션`](superpowers/plans/2026-05-22-W10-E-test-and-data-migration.md) — `scripts/db/wipe-saa-test.sh` 가드(`saa_test` prefix 강제) + `db-ops.md §3.4` W10 pre-flight 런북, `tests/api/conftest.py` `active_project`/`active_material`/`active_scan` fixtures rewire, 27 backend test 파일 4 batches(3a projects+deps / 3b run+SSE / 3c data+static / 3d selector+scans+mutex+guards), 신규 `tests/api/test_w10_acceptance.py` 6 specs(D2 DELETE-RESTRICT × 2 / D4 scan-mutex × 2 / D6 force-create × 2 — 모두 green; 423 Locked status code, ProjectHandle response shape, /run/fake mini-app substitution 사용), `scripts/dev/w10-acceptance.sh` 게이트(alembic+pytest+vitest+build). **완료(2026-05-25, 7 commits `bc7f6ea`..`396f630`).** 별건 회귀: `test_scans_*` 13건 NotNullViolationError on `scans.project_id`(W10-A FK 도입 후 `upload_service.create_scan`이 `project_id` 미전파) — #64로 분리 추적, 1차 목표 마지막 블로커.
- [x] **W12** ScanTable + scan delete (UI + backend) — [`plan`](superpowers/plans/2026-05-26-W12-scan-table-and-delete.md) — `s3_cleanup.delete_prefix` 헬퍼(paginated 1000-key batch) + `DELETE /scans/{scan_id}` 라우트(editor 가드 → S3 prefix wipe → ORM cascade), `ScanPicker` 드롭다운 → `ScanTable` 6-column 정렬 가능 + 인라인 delete confirm. **완료(2026-05-26, T1-T12 + IAM fix, owner smoke PASS)**.
- [x] **W11** scan access guards + new-scan-only 정책 codify — [`plan`](superpowers/plans/2026-05-26-W11-scan-guards.md) — Phase E5 gap 후속. `get_scan_for_user` / `require_editor_for_scan` 헬퍼 추가, 4개 scan-level 라우트 + `list_scans`/`create_scan`에 wire, outsider→404(leak 방지) / viewer→403. `UploadModal` 상단에 new-scan-only 정책 docstring(자동 resume UX는 범위 외 명시). **완료(2026-05-26, 9 task subagent-driven, T1 `ac52d73` Forbidden / T2 `df2bf82` get_project_for_user / T3 `8bb6101` scans_service guards / T4 `9133577` presign / T5 `661c146` complete / T6 `dd40307` finalize / T7 `d0a7691` get_scan+list+create / T8 `23db99d` UploadModal docstring / T9 본 문서 갱신).**
- [ ] **Compute Pipeline** (param form + GPU SAM + workflow runner) — [`plan`](superpowers/plans/2026-05-25-segmentation-web-integration.md) — brainstorm 8개 결정 잠금(2026-05-26). 기존 segmentation-web-integration 플랜에 **Phase 0**(decision lock) + **Phase 5**(Pipeline UX 5 task) 흡수. 실행 순서: Phase 1(SAM port — algo-engineer) → Phase 2(runs wiring — api-developer) → Phase 5(orchestrator + UX — api-developer + frontend-architect) → Phase 3(frontend SAM 패널) → Phase 4(GPU EC2 — devops-engineer, owner 승인 게이트).
- [~] **W7** GPU 워커 트리거 — [`plan (SKETCH, SUPERSEDED)`](superpowers/plans/2026-05-21-W7-gpu-workers.md) — **segmentation-web-integration 플랜으로 흡수**. D1-D7 결정은 신규 플랜 Phase 4에 통합.
- [x] **W8 (Part 1)** CI: alembic 모델↔스키마 drift check — [`plan (READY, Part 1)`](superpowers/plans/2026-05-21-W8-ci-drift-and-ssm.md) — devops-engineer (or api-developer), 3 tasks. 새 워크플로 `.github/workflows/alembic-drift.yml`. (완료 — `0715a41`)
- [ ] **W8 (Part 2)** SSM Session Manager 검토 (SSH key 배포 없이 DB 접근) — 같은 플랜 §"Part 2 (SKETCH)" — D2.1–D2.3 결정 필요.
- [ ] **W9 (W4.4 후속)** 실제 annotated 데이터로 `reg_covar` 캘리브레이션 sweep — [`plan (SKETCH)`](superpowers/plans/2026-05-21-W9-reg-covar-calibration.md) — D1 (데이터셋 소스)이 핵심 블로커. fog/overlap에서 leak 발견 시 `auto_tune_reg_covar` 2D 그리드로 확장.

---

## 4. 미해결 결정 / 사용자 승인 대기

- **W6.1 Cognito 부트스트랩 (AWS state 변경)** — `scripts/devops/cognito_bootstrap.sh` 실행 권한 + alembic `0002_v7_auth` RDS 적용 + SSM 파라미터(`/saa/cognito/*`) 등록. 절차는 [`docs/cognito-setup.md`](cognito-setup.md). 비용은 Cognito MAU 기반(~$0.0055/MAU, free tier 50k).

---

## 5. 알려진 제약 / 메모

- **alembic `--autogenerate` 금지.** GENERATED column / composite FK / partial index / ENUM 제대로 못 잡음. 마이그레이션은 손으로. (이유: `db-ops.md` §3)
- **스키마 변경은 새 v.** v6 동결, breaking change 시 `db-schema-v7.md` + 새 revision.
- **Bastion public IP는 stop/start마다 바뀜.** 운영 절차는 `db-ops.md` §2.
- **오너 home/office IP 바뀌면** bastion SG ingress 갱신 필요 (`db-ops.md` §2.3).
- Streamlit (`app/streamlit_app.py`)은 당분간 legacy로 유지. 마이그레이션 검증용.

---

## 6. 인프라 핵심 ID (빠른 참조)

자세한 건 [`db-ops.md`](db-ops.md) §1.

| | |
|---|---|
| AWS profile / region | `qpress` / `us-east-2` |
| RDS endpoint | `qpressdb.ch08y4ooqgmq.us-east-2.rds.amazonaws.com:5432` |
| App DB | `qpress` (master user `houk`, 비번은 Secrets Manager) |
| Bastion EC2 | `i-063165d449976b2e4` (현재 `stopped`) |
| Bastion key | `~/.ssh/qpress-bastion.pem` |

---

## 7. 변경 로그 (이 문서 자체)

- 2026-05-21: 초기 작성. CLAUDE.md에서 "어디까지 와 있나" 섹션 분리.
- 2026-05-21: 영속 에이전트 7개 부트스트랩 — `.claude/agents/*.md` + CLAUDE.md §3 갱신.
- 2026-05-21: W4.2 완료 — ORM `Computed` 수정 + `db.reconcile` 헬퍼. manifest 엔드포인트 rewire는 W2.1로 이연.
- 2026-05-21: W4.4 완료 — `reg_covar` tunable + auto-opt 드라이버 + parity golden 재생성(owner 승인). 디폴트 `1.0` → `10.0`(rank-deficient 시드 covariance 정규화). 다음은 W3.5 UI follow-up.
- 2026-05-21: 백로그 7개 항목(W3.5 / W2.4 / W5 / W6 / W7 / W8 / W9) 전체 플랜 작성 — detailed 2개(W3.5, W2.4, W8 Part 1), sketch 5개(W5/W6/W7/W8 Part 2/W9). 실행 가능 플랜은 subagent-driven-development로 dispatch 가능, sketch는 D-block 결정 필요.
- 2026-05-21: **W8 Part 1** 완료 — alembic drift CI workflow (`0bbf448` / `0715a41` / `623c5af`). `python scripts/check_alembic_drift.py`가 PR마다 ephemeral Postgres에 마이그레이션 적용 후 `compare_metadata` 비교. 모델/스키마 drift 시 build 실패.
- 2026-05-21: **W2.4** 완료 — `GET /data/manifest`가 DB의 step status를 source of truth로 오버레이 (`6fde904` / `63467ab` / `f746e74` / `d530ff7` / `2d5d7d5`). 디스크 manifest는 file paths · 비-DB step (clustering/selector/thumbnails/explorer) 만 유지. DB row 없으면 silent fallback, SQL error는 `db_unavailable` 500.
- 2026-05-21: **W3.5** 완료 — backend slice (`e082d90` / `268ac65`) + frontend slice 7 commits (`ddd33c7`..`ab87bf9`). `reg_covar` 슬라이더([0.1, 10.0] log-scale, default 10.0) + AutoTune 버튼이 `ClusteringRightRail.tsx`에 마운트. data-testids: `clustering-reg-covar-slider` · `clustering-reg-covar-value` · `clustering-auto-tune`. SSE `done` 페이로드에 `reg_covar_chosen` 포함. 246 vitest tests pass.
- 2026-05-22: **W6 Test B (로컬 PG 통합 검증)** 완료 — 5 commits (`1366dc3` / `620af7a` / `e53c169` / `4591bde` / `8e09f2a`). `app.dependency_overrides[get_db_session]`로 라우트와 테스트가 `pg_session` 공유, role-mismatch 테스트는 `get_current_user` 직접 오버라이드. PG-marked tests/api 31/31 pass, tests/db 26/26 pass, 비-PG 385/385 pass (4 pre-existing `tests/test_xaccel_thumbnails.py` 실패는 W6 auth-gate 도입 시점에 이미 있던 것 — 별도 백로그). 다음은 W6 Test C (Cognito 부트스트랩 + e2e 로그인).
- 2026-05-22: **W5 D-block 잠금** + **5개 plan 작성**: D1 단일 버킷+dev/prod prefix / D2 브라우저 SHA256 + `x-amz-checksum-sha256` / D3 per-scan 세션 / D4 사용자 입력 폼(material 컨트롤드 보캐브 + auto-add, ix/iy 0-based 필수, image_count, extra_metadata JSONB) / D5 dev-bypass+Cognito 병행 / D6 프로젝트 상세 페이지 모달. `1 project = 1 scan v1` 결정으로 `scans.project_id` 컬럼 미추가(path-only routing). plan suite: W5-A(스키마) / W5-B1(materials+scan-create) / W5-B2(presign+complete+finalize) / W5-C(프론트엔드) / W5-infra(AWS S3). 옛 `2026-05-21-W5-upload-flow.md`는 Superseded.
- 2026-05-22: **CLAUDE.md** §2.5에 PM Bash 룰 + background 디폴트 룰 추가, §5에 Self-audit 단계(step 7) 추가. PM이 도메인 명령(pytest/uv run/npm/alembic) 직접 실행 금지, 에이전트 호출 디폴트 background. 사용자 피드백 대응(2건의 PM 룰 위반 — pytest 직접 실행 + foreground agent dispatch).
- 2026-05-21: **W6 전체** 완료 (코드, 28 commits) — Cognito + 4-tier 글로벌 ENUM(member/reader/operator/admin) + per-project ACL(viewer/editor) + usage events + 프론트엔드. 6 sub-plan: **W6.0** schema v7 (`8bf3c90`..`c51fba0`, UUID users + ENUMs + project_users + usage_events + alembic `0002_v7_auth`), **W6.1** Cognito 인프라 docs (`ad645cf` / `8dd41ea`, 부트스트랩/스모크 스크립트 + 런북 — AWS 실행은 승인 대기), **W6.2** 백엔드 의존성 (`fefd45e`..`f4aeb16`, JWKS verifier + cognito_sub upsert + dev-bypass + /auth/me·callback·logout), **W6.3** ACL (`57445f5`..`7c75ece`, 순수 resolver + require_role/require_project_role 가드 + admin 라우트), **W6.4** usage events (`33d1c5f`..`f53f36f`, emit 헬퍼 + login/logout/scan_run 훅 + GET /admin/usage), **W6.5** 프론트엔드 (`0518258`..`bd9339b`, authSlice + LoginPage/SignupPage/AdminPage + RequireAuth/RequireRole + 사이드바 + 모든 fetch에 Bearer 토큰). 백엔드 비-PG 292 pass, vitest 265 pass. 다음: W6.1 Cognito 부트스트랩 AWS 승인.
- 2026-05-22: **W5-A** 완료 — 4 commits (`1067eac` Material ORM / `d91ffff` alembic 0003 + 5-row 시드 / `2868b3f` Scan/Image ORM tightening / `70064dd` v7.1 doc). 로컬 saa_test에 적용 + round-trip 검증 통과. PG `tests/db` 21/21 pass. 스키마 v7.1: `materials(name PK, created_by_id FK, created_at)` + 시드 5종(graphene/MoS2/WSe2/hBN/WS2) + `scans.extra_metadata JSONB` + `scans.material NOT NULL+FK ON DELETE RESTRICT`(partial index 제거) + `images.grid_ix/iy NOT NULL` + `images_scan_grid_uq UNIQUE(scan_id, grid_ix, grid_iy)`(partial index 제거). RDS 적용은 W6.1 Cognito 승인 시점에 함께. anomaly: admin_usage 테스트 3건 격리 이슈는 W5-A 무관(별도 백로그).
- 2026-05-22: **W5-infra** 완료 — 6 prep commits (`140b757`..`3a69369`) + AWS apply 7 게이트 모두 적용. 버킷 `qpress-uploads` (us-east-2, SSE-S3+BucketKey, BPA 4 flags, BucketOwnerEnforced) + dev-only CORS(localhost:5173/5174) + lifecycle 3 rules(dev/ 30d, multipart 7d, dev/uploads-pending/ 1d) + IAM policies(`qpress-api-s3-uploads-dev`/`prod`) + IAM user `qpress-dev-local` + access key(`~/.aws/credentials` `[qpress-dev-local]` 프로파일) + bucket policy(cross-prefix deny 3 statements). `scripts/s3/dryrun.sh` 14/14 PASS. prod 도메인 결정 시 CORS 추가 예정. 다음: W5-B1 (materials + scan create endpoints).
- 2026-05-22: **admin_usage 테스트 격리 이슈 해결** — `21e9640` (`tests/db/conftest.py` only, +15/-2). `pg_session`에 `join_transaction_mode="create_savepoint"` 추가. SQLAlchemy 2.x async session이 outer trans 없이 bind된 상태에서 `commit()` 호출 시 outer trans를 deassociate시켜 rollback이 무효화되던 SAWarning이 root cause. `usage_events` 누적으로 `tests/api/test_admin_usage_route.py` 3건 실패하던 것 PASS. 프로덕션 코드 무변경. `tests/api -m pg` 35→38 pass, `tests/db -m pg` 21/21 (SAWarning 사라짐). 부수 발견 백로그(별건): #64 `test_scans_*` 11 failures (KeyError `s3_uri`), #65 `tests/scripts/conftest.py` `drop_all` footgun (조사 중 saa_test 스키마 한 번 wipe — 수동 복구), #66 `test_run_*_sse.py` hang (DB 미사용, asyncio teardown 의심).
- 2026-05-22: **W10 D-block 잠금** + **5개 plan 작성**: D1 real `projects` 테이블 (TEXT PK = `gen_random_uuid()::text`, name UNIQUE per-owner) / D2 DELETE RESTRICT (scans 존재 시 409, 명시적 wipe 필요) / D3 in-memory persistence only (no localStorage active project) / D4 `asyncio.Lock` per-scan mutex (다른 scan 동시 실행 OK, 같은 scan 두 번째 호출 409) / D5 per-scan filesystem `<root>/<pid>/<sid>/manifest.json` + `SAA_ANALYSIS_FOLDER` → `SAA_ANALYSIS_ROOT` rename / D6 zero-projects day-zero UX (Sidebar "+" only enabled, 모든 탭 disabled, 첫 프로젝트 생성 후 자동 이동). plan suite: W10-A(스키마+alembic 0004) / W10-B(active project decoupling + per-scan mutex/manifest) / W10-C(route surface `/projects/{pid}/scans/{sid}/...` migration) / W10-D(프론트엔드 routes + ScanPicker + CreateProjectModal + 18 vitest 3 parallel batches) / W10-E(test sweep 27 files 4 parallel batches + acceptance gate `scripts/dev/w10-acceptance.sh`). 판단 콜: TEXT PK(UUID 가독성+postgres 호환), 1 project = N scans (W5 `1 project = 1 scan v1` 폐기), in-memory only (URL이 source of truth), scan picker는 탭 콘텐츠 상단 (Sidebar에 두지 않음 — 프로젝트 선택과 분리), test sweep은 batch 단위 parallel dispatch (27 + 18 파일 단일 호출 시 토큰 폭발).
- 2026-05-22 — W10-A 스키마 (projects 테이블 + scans/project_users FK 재배선) 완료. alembic head `0004_w10_projects`. tests/db 일부 회귀 10건 (Scan() 생성자에 project_id 누락 — `test_analysis_status_generated.py` 9건 + `test_w5a_schema.py::test_image_grid_uniqueness` 1건) — W10-E sweep에서 처리. W10-B (active-project decoupling) 진입 가능.
- 2026-05-23 — **W10-B** (active-project decoupling + per-scan manifest/mutex) 완료. 4 commits: `50197a4` paths 헬퍼 + `SAA_ANALYSIS_ROOT` env (legacy `SAA_ANALYSIS_FOLDER` fallback 1릴리즈 유지) / `cf808a5` per-scan manifest 래퍼 (`load_for_scan`/`save_for_scan`) / `e1c9671` `acquire_scan_lock(scan_id)` per-scan asyncio.Lock / `8de71f9` `api/deps.py` 재작성. `_active_project` 글로벌 / `DEFAULT_PROJECT_ID` / `ProjectContext` / `get_project_context` 전부 제거. 새 시그니처: `get_manifest(project_id, scan_id)` · `get_active_analysis(scan_id, session)` · `acquire_scan_lock(scan_id)`. 21 tests green (state 13 + deps 4 + mutex 2 + scan_mutex_isolation 2). 라우트 레이어에서 `DEFAULT_ANALYSIS_FOLDER` 등 삭제된 심볼 import로 `tests/api -m pg` 42 파일 collection ImportError — W10-C scope. 다음: **W10-C** (route surface `/projects/{pid}/scans/{sid}/...` 재작성).
- 2026-05-23 — **W10-C** (route surface migration) 완료. 7 commits: `9fcae04` projects CRUD service + schemas (Task 1) / `54f88a7` projects CRUD route surface (Task 2) / `30c33d0` `GET /projects/{pid}/scans` list endpoint (Task 3) / `7bd358d` data + static routers per-scan grammar (Task 4a) / `c17bbfc` run + selector routers per-scan grammar + per-scan mutex via `acquire_scan_lock(scan_id)` (Task 4b) / `f525804` clustering + explorer routers per-scan grammar (Task 4c) / `02abe6f` drop stale `ValidatePathsRequest` import in `tests/api/test_schemas.py` (Task 1 follow-up). 모든 도메인 라우터(`/projects/{pid}/...`)가 `/projects/{pid}/scans/{sid}/...` 그래머로 이동, per-project mutex → per-scan mutex 교체. 라우트 총 52개. 5.1 PG sweep 2 pass / 87 skip / 0 fail (PG-marked 스위트 PG infra 없이 실행). 5.2 비-PG sweep 205 pass / 5 fail — **5건 모두 W10-E scope의 stale 테스트 파일**(W10-E 플랜 §"4 batches"에 명시): `test_manifest_endpoint_db.py` (3, 레거시 `/projects/local/data/manifest` 사용 → per-scan 그래머로 이주 예정), `test_path_validation.py` (1, W10-C에서 정당하게 삭제된 `validate-paths` 라우트 — W10-E에서 `git rm`), `test_sse_heartbeat.py::test_run_thumbnails_emits_heartbeat_when_pipeline_is_slow` (1, 레거시 `/projects/local/run/thumbnails` 사용 → 이주 예정). W10-C 자체에는 회귀 없음, 잔여 stale 테스트는 W10-E에서 일괄 처리. 다음: **W10-D** (frontend) 진입 가능.
- 2026-05-25 — **W10-D** (frontend) 완료. 8 commits `5314879`..`044e64d`: `5314879` `projectSlice.ts` `activeScanId` + cross-project clear / `aa3743f` `api/projects.ts` CRUD-only rewrite + `listScansForProject` / `0881f03` `CreateProjectModal`(name+description; legacy `CreateProjectForm` 대체) / `6a2756f` `ScanPicker` 드롭다운 + empty-state CTA / `1cbbeb2` `App.tsx` `/projects/:pid/scans/:sid/<tab>` 그래머 + `ProjectScanSync` / `a465aca` 탭 페이지 `scanId` prop + `ComputeTab` no-scan empty state / `b147836`+`3d7a771`+`2e3f9c6` 18 vitest 파일 3 batches(selector/components, clustering, explorer/uploadmodal) / `044e64d` Sidebar `listProjects` + `CreateProjectModal` 통합. 최종: vitest 86 files / 300 tests pass, `npm run build` green. Task 9(수동 e2e)는 owner 검증 단계로 이연.
- 2026-05-25 — **W10-E** (test+data migration) 완료. 7 commits `bc7f6ea`..`396f630`: `bc7f6ea` `scripts/db/wipe-saa-test.sh` 가드 wrapper(`saa_test` prefix 강제) + `db-ops.md §3.4` W10 pre-flight 런북 / `a6709de` `tests/api/conftest.py` `active_project`/`active_material`/`active_scan` fixtures rewire(`_active_project` 글로벌 폐기) / `3df08ce` batch 3a(projects+deps+project_context) / `2c3f39e` batch 3b(run+SSE 라우트) / `7f449a0` batch 3c(data+static 라우트) / `8cb48d1` batch 3d(selector+scans+mutex+guards) / `439ca61` 신규 `tests/api/test_w10_acceptance.py` 6 specs(D2 DELETE-RESTRICT × 2 / D4 scan-mutex × 2 / D6 force-create × 2 — 모두 green) / `396f630` `scripts/dev/w10-acceptance.sh` 게이트 스크립트(alembic upgrade head + pytest + vitest + npm build). 검증 콜: ① D4 `/run/fake` substitution(production `/run/fake` 라우트 부재로 mini-app에 합성 엔드포인트 마운트, `acquire_scan_lock(scan_id)`만 약 150ms 보유) ② status code 423 Locked(409 아님 — `ProjectBusy.status_code = HTTP_423_LOCKED` errors.py:64) ③ D6 응답 shape: `POST /projects` → `ProjectHandle`(no `scan_count`); `scan_count`는 `GET /projects/{pid}` `ProjectDetail`에만. 부수 발견 별건 회귀: `test_scans_*` 13건 NotNullViolationError on `scans.project_id`(W10-A FK 도입 후 `upload_service.create_scan`이 path arg `project_id`를 안 넘김) — #64로 분리 추적.
- 2026-05-26 — **Upload Robustness Phase A→D** 전부 완료. 플랜: [`2026-05-26-upload-robustness.md`](superpowers/plans/2026-05-26-upload-robustness.md). **Phase A** (visibility, 4 tasks): aggregate counter / failed-only filter / request_id surfacing / 백엔드 모듈 logger + structured 로그. **Phase B** (server reliability, 6 tasks): presign 멱등성(같은 sha256+filename+grid → 200 same upload_item_id) / startup S3 bucket 체크 / `complete()` head_object를 `run_in_executor`로 / `cancelAll` reset / `PRESIGN_TTL_SECONDS=300` 단일 상수 / **`raise HTTPException` → ErrorEnvelope 일괄 통일** (request_id 노출 보장). **Phase C** (1 task): `StepCard`가 scanId를 `useStepProgress`까지 전달, SSE URL이 `/projects/{pid}/scans/{sid}/run/{step}` 그래머로 들어감 — Compute Tab 404 해결. **Phase D** (5 tasks): D1 `Scan.status`(`'draft'|'ready'`) + alembic 0005 + list endpoint이 `uploaded_count`(correlated subquery) + `status` 노출 / D2 ScanPicker 라벨 `name (uploaded/expected · status)` truthful 화 / D3 모달 close confirm + AbortController로 createScan 중단(stale write race 방지) + scanId 보존 / D4 `retryAllFailed` 오케스트레이터 메서드 + UI 버튼 / D5 materials cache invalidation regression test (실 invalidation은 W5-C.3 `996accc`에서 이미 들어가 있었음, D5는 lock-in 테스트만 추가). 13 commits `ac52898`..`a10491c` (마지막 `a10491c`는 D5 nit fix — `toHaveBeenCalledTimes(2)` → `>=2`로 brittle 완화). 343/343 vitest pass, typecheck clean. **다음: Phase E** (real-data 검증 루프) — backend restart 필요. E1(1) → E2(10) → E3(10+1 corrupt) → E4(50 cancel/resume) → E5(20 close-reopen) 통과 후 **E6 3648 PUT × ~9 GB**는 오너 승인 후 실행.
- 2026-05-26 — **W11 scan-guards 완료** + **Hotfix #10 subfolder toggle**. W11: 9 task subagent-driven TDD, 8 commits `ac52d73`..`d0a7691` + docstring `23db99d`. T1 `Forbidden(AppError, status=403, code=forbidden)` envelope (`src/flake_analysis/api/errors.py`) / T2 `get_project_for_user(session, *, project_id, user, require_editor)` 헬퍼 — outsider→`ProjectNotFound` 404, in-project viewer w/ `require_editor=True`→`Forbidden(action=project_edit, project_id=...)` 403 / T3 `services/scans.py` 신규 모듈: `_resolve_effective` + `get_scan_for_user` (read) + `require_editor_for_scan` (write) — `acl.resolve_effective_project_role(user.role, is_owner, acl_role)` 활용 / T4-T6 `presign_image_put` / `complete_image` / `finalize_scan` 가드 wire (외부인 → `ScanNotFound` 404, leak 방지) / T7 `create_scan` (`require_editor=True`) + `list_scans_for_project` (top-of-body guard, return discarded) + `get_scan` (read-only) wire. `tests/api/test_scan_access_guards.py` 6/6 PASS (3 outsider 404 × `presign`/`complete`/`finalize`/`get`/`list` + 1 viewer-can-read-but-not-finalize 403). T8 `UploadModal.tsx` line 20-34 docstring으로 new-scan-only 정책 lock-in (frontend 345/345 vitest, typecheck clean). 정책 근거: client에서 `upload_items` reconcile하는 resume UX는 비용 대비 가치 낮음, 별도 brainstorm 후 plan으로만 진입. 별건 발견(W11 무관, 베이스라인 동일): `tests/api/`에 15-17건 pre-existing flaky failures (admin_usage 4 / auth_dep_dropin 1 / scans_complete 4 / scans_finalize 3 / scans_list 2 / scans_presign 1 / run SSE concurrency 3) — order-dependent, S3 bucket 가정, dev-bypass user state pollution. Hotfix #10: `FileDropzone`에 `data-testid=file-dropzone-include-sub` 체크박스 (default OFF), `walkEntry(entry, recurse, depth)`가 `!recurse && depth >= 1 && entry.isDirectory`일 때 빈 배열 반환 (drop 경로); picker 경로는 `webkitRelativePath`에 `/`가 두 번 이상이면 skip. E6 1730/5378 failures의 원인이 사용자가 모르고 root에 있던 subfolder의 이미지들까지 드롭되며 grid_ix/grid_iy 정규식 실패였음. 343→345 vitest. 다음 한 발: ① **CORS AWS apply** (`infra/s3/cors.json` `1aa0014` 커밋만 된 상태, S3 적용은 오너 게이트, devops-engineer 수행) → ② **E6 3648/9GB** 오너 승인.
- 2026-05-26 — **E6 풀세트 PASS — 1차 목표 도달**. owner가 `test/test` 프로젝트로 3648 PNG / ~9 GB 풀세트 업로드 검증. W12 ScanTable / 인라인 delete / IAM 적용 후 라이브 동작 확인까지 동일 세션에서 끝남. Phase E real-data 루프(E1 single → E2 cancel → E3 retry-failed → E4 reopen-partial → E5 wrong-project guard 식별→W11 → E6 풀세트) 전체 그린. cutover 1차 목표(Streamlit standalone → React+FastAPI+PG 업로드 + scan 라이프사이클)는 여기서 클로즈. 다음 큰 한 발은 W7 GPU 워커(D1-D7 결정 필요) — 별도 brainstorm 진입.
- 2026-05-26 — **Compute Pipeline brainstorm — 8개 결정 잠금 + 기존 플랜 흡수**. owner와 4단계 AskUserQuestion sweep. (1) UX 원클릭 풀 파이프라인 + 5스텝 타임라인. (2) 파라미터는 스캔당 1폼. (3) 재실행 분기 그래프(Thumbnails 독립, Background → SAM → {Stats, Prox}). (4) GPU = 온디맨드 spot. (5) SAM 포트 포함. (6) 1 scan = 1 default analysis(v1). (7) 파라미터 저장 = `analyses.{amg,background,proximity}_params` JSONB(기존 v6). (8) Background cascade 자동 + UI 도장. 리서치 결과 기존 [`2026-05-25-segmentation-web-integration.md`](superpowers/plans/2026-05-25-segmentation-web-integration.md) 플랜(1898줄)이 ~80% 커버 — 6개 W13 sub-plan을 신규 작성하는 대신 **기존 플랜에 Phase 0(decision lock) + Phase 5(Pipeline UX 5 task) 추가**로 결정. Phase 5 task 풀세트: P5.1 `scans.thumbnail_params` JSONB 옵션(PM mini-sweep) / P5.2 `POST /run/pipeline` 오케스트레이터(`PipelineProgressBridge` SSE 래퍼 + `get_or_create_default_analysis` + `apply_background_cascade_if_needed` Convention #4 enforce) / P5.3 frontend `usePipelineProgress` + `PipelineParamsForm` + `PipelineTimeline` / P5.4 ComputeTab 재설계 + `CascadeConfirmDialog`(Background dirty → "Will rerun: SAM, Stats, Prox" 확인) / P5.5 acceptance gate. 실행 순서: Phase 1 → Phase 2 → Phase 5 → Phase 3 → Phase 4. W7 sketch SUPERSEDED 헤더 P1.0 사양에 맞게 정렬(`SUPERSEDED by 2026-05-25-segmentation-web-integration.md`).
- 2026-05-26 — **CORS AWS apply** — `infra/s3/cors.json`(`1aa0014`로 `127.0.0.1:5173/5174` 추가)을 라이브 `qpress-uploads` 버킷에 적용. devops-architect 위임. 의외 발견: pre-apply 시점에 라이브 버킷이 이미 4 origins 상태 — 어느 시점에 조용히 적용됐던 것으로 보임(콘솔 수정 또는 이전 세션). put은 idempotent re-apply로 안전, drift 없음, single CORSRule(`qpress-uploads-browser-put`) + MaxAgeSeconds=3000 유지. 결론: 코드/AWS 정합 확인 완료, `127.0.0.1` 우회 추가 검증 불필요.
- 2026-05-26 — **W12 IAM hotfix** — owner T11 smoke 첫 시도가 `scan_delete_s3_failure` 500으로 실패. 근본 원인: `infra/s3/iam-policy-dev.json`(W5-infra 시점 작성)이 PUT/GET만 허용하고 `s3:DeleteObject` 누락 → `delete_objects` per-key AccessDenied → `s3_cleanup.delete_prefix`가 `RuntimeError` raise → 라우트가 `ScanDeleteS3Failure` (500) 반환. 버킷 정책은 DeleteObject deny 없음 — IAM identity policy만 게이트였음. 로컬 fix `4eda2fc` (dev + prod 정책 모두 `s3:DeleteObject` 추가, `Resource: arn:.../dev/*` & `arn:.../prod/*` scope 유지로 cross-prefix 보호 그대로). 라이브 적용: managed policy `qpress-api-s3-uploads-dev` v2 default 셋업 (`aws iam create-policy-version --set-as-default`, devops-architect 위임). 재시도 후 owner smoke PASS. 교훈: 새 라우트가 새 IAM action 요구할 때 인프라 정책 갱신을 task plan에 명시 — Phase E6 / 향후 prod cutover 전 prod 정책 라이브 apply도 빼먹지 말기.
- 2026-05-28 — **#211 8-GPU 재측정 사전작업 완료, 측정 dispatch는 다음 세션으로 이연**. 핸드오프 문서: [`docs/handoff-2026-05-28.md`](handoff-2026-05-28.md). 세션 흐름: #221 RCA(AMI bootstrap 4 BLOCKER + 2 DEGRADED + 2 LATENT) → #222 `sam-bake-ami.sh` 빌더 작성(`c931385`) → #225 PAT/IAM pre-check PROCEED → #226 첫 bake 실패(spot capacity drought + apt 실패 + SSM 24KB 절단) → #227 빌더 패치 2종(`42c960d` spot→on-demand 6단 fallback + S3 영속 로그 / `62f272a` awscli 사전 설치 — Ubuntu cloud image 미포함) → #228 DLAMI 전환 + 9 fix-forward(DLAMI base, bash-3.2, HOME=/root, PAT via `git -c`, vendor gitlink bump+cherry-pick `505e1cb`, spot stop skip, CreateImage description trim, AMI wait extend, sync before snapshot). 최종 AMI **`ami-092ae5880cb9cf957`** Status=Validated, Base=DLAMI Ubuntu 22.04 OSS(CUDA 12.9 / driver 580.159.04 / peft 0.19.1 / torch 2.12.0+cu130). 누적 비용 ~$0.59 / $100 cap. PM 룰 위반 2건 자체 보고: (a) 에이전트 runtime type 라우팅 실수(`devops-architect` Qpress 도메인 → `general-purpose` + 정의 파일 로드 패턴이 표준) (b) `sam-bake-ami.sh` 첫 디자인에 spot→on-demand auto-fallback 누락(오너 지적 후 `42c960d`로 박음). 다음 세션 한 발: **#229 (8-GPU 100장 측정, AMI 준비됨, boot/load/processing 분리 계측 필수)**.
- 2026-05-26 — **W12 ScanTable + Delete** 코드 완료(T1-T10). 12-task subagent-driven TDD plan ([`plan`](superpowers/plans/2026-05-26-W12-scan-table-and-delete.md)). Backend 4 commits: T1 `s3_cleanup.delete_prefix(*, bucket, prefix)` 헬퍼 (boto3 `list_objects_v2`+`delete_objects`, 1000-key page batching, moto `mock_aws` 4 tests covering 0-keys/<1000/>1000/error-fast-fail) + T2 `DELETE /scans/{scan_id}` skeleton (`require_editor_for_scan` 가드, outsider→404 `scan_not_found`) + T3 body wires S3 wipe → DB delete (ORM cascade로 images/upload_sessions/upload_items/analyses 자동 삭제, JSONB-only `usage_events`는 FK 없어 이력 보존) + T4 backend regression sweep(test_scan_delete 8/8 + 인접 모듈 회귀 0). Frontend 5 commits: T5 `deleteScan(scanId: number)` API client (`Accept: application/json` + `forbidden`/`scan_edit` 에러 envelope unwrapping, 2 vitest) + T6 `ScanTable.tsx` 6-column skeleton (name/material/images/status/created/actions, empty state) + T7 column-click sort (asc/desc 토글 + ▲/▼ 인디케이터, localeCompare for strings, numeric for images, Date.parse for created) + T8 인라인 delete 버튼 + confirm 다이얼로그 + `useMutation`+invalidation + sonner toast 성공/실패 + `stopPropagation` 체인으로 row navigation 차단 + `data-testid='scan-table-confirm-${id}'` + `disabled={del.isPending}` 더블파이어 가드 + 7 vitest (render/empty/sort×2/delete-happy/cancel/failure) + T9 `ScanPicker.tsx`를 6라인 re-export shim으로 교체 (`export { ScanTable as ScanPicker } from './ScanTable'`) — 4개 탭 페이지 무변경(원자적 swap). 검증: T10 `npx tsc --noEmit` 0 errors / `npm run build` 15.80s 성공 / vitest 350/350 pass. 잔여: T11 manual smoke(오너 검증), T12(본 항목). 발견 plan defects(별건 학습): jest-dom 미설치 → native Chai만 사용 / `wrap()` URL의 빈 `${sid}` 세그먼트(`/scans//compute`)가 React Router v6에서 매치 실패 → `sid` falsy일 때 `/projects/${pid}` 폴백 라우트 사용 / Rules of Hooks(useState가 early return 뒤에 있으면 안 됨) → useState/useMutation을 컴포넌트 최상단으로 호이스트 / `vi.fn()` 호출 이력이 같은 파일 내 테스트 간 누수 → `mockReset()` + 즉시 re-stub / `Response.json()` body 단일 소비 → `mockImplementation(() => Promise.resolve(new Response(...)))`로 매 호출 fresh Response. 5개 모두 다음 task 브리프에 사전 플래그로 회귀 차단.
- 2026-05-26 — **Phase E real-data 검증(E1–E5)** 완료(scan_id 287..291, S3 ~151 objects in `qpress-uploads/dev/scans/`). E1 single-file ✅ / E2 cancel mid-flight ✅(throttled fetch + Stop & Close, 서버측 draft 보존) / E3 retry-failed ✅(synthetic 500 on PUT #5/#10/#15 → 47 done · 3 failed → "Retry failed (3)" → 50/50 ready → finalized) / E4 reopen-partial ✅(E2 결과로 합산 검증, picker가 `(44/50 · draft)` 정확히 표시) / E5 wrong-project guard 식별만 — 서버측 라우트 4개(`presign_image_put`/`complete_image`/`finalize_scan`/`get_scan`)가 caller↔project 멤버십을 검증 안 함을 확인, **W11 plan으로 분리**. CORS는 `localhost:5173` 우회로 검증 진행, `infra/s3/cors.json`에 `127.0.0.1:5173/5174` 추가만 커밋(`1aa0014`)됨 — AWS apply는 owner-gated. plans 인덱스 정리 커밋 `28be730`. **E6**(3648 PNG / ~9GB)는 오너 승인 대기. 별도 plan 작성: [`2026-05-26-W11-scan-guards.md`](superpowers/plans/2026-05-26-W11-scan-guards.md) `c5497ec` — 9 task TDD(Forbidden 에러 envelope / `get_project_for_user` / `get_scan_for_user`+`require_editor_for_scan` / 4 라우트 wire / list_scans+create_scan wire / UploadModal new-scan-only docstring / project-status 갱신). **정책 lock-in**: UploadModal은 new-scan-only — drop된 client-side 상태와 server-side `upload_items`를 reconcile하는 resume UX는 비용 대비 가치가 낮아 본 milestone 범위 외, 별도 brainstorm 후 plan으로만 진입.
- 2026-05-28 — **8-GPU measurement run — SMOKE_FAIL** ([plan](superpowers/plans/2026-05-28-sam-8gpu-parallel.md) Tasks 5-8). `g6e.48xlarge` spot `i-038703b1a1740faad` 13분 가동 → 10-image smoke 62.6s만에 `FileNotFoundError: '../external/sam2'`. 근본 원인: vendor `run_multi_process` → `build_sam2_finetuned` 경로가 `sam2_repo` directory + `args.json` + base SAM2 ckpt + LoRA 어댑터 mount를 요구하는데 우리 `merged.pt`는 single-GPU `state["model_config"]` shortcut 전용 포맷(이마저 `model_config={}` 빈 dict — 단일 GPU 경로도 미검증). plan Risk #4가 예측한 config-key drift보다 큰 architecture mismatch. 인스턴스 종료 ~04:21Z, 비용 ~$0.92($1.50 hard cap 이하). 별건 부수 작업: 워커 부팅 fix 2건 cherry-pick (`d5d9783` `App.open_async()` / `00b997f` `psycopg_pool kwargs=`) — `worktree-agent-a7087c671a2ac8601`에만 있던 fix를 `feat/migration-cutover`에 이식. **권고**: algo-engineer에게 multi-GPU adapter 결정 라우팅 (옵션 1 자체 spawn-pool / 2 vendor 패치 / 3 weight 재머지). 단일 GPU 3.98 s/img baseline도 real-GPU 재검증 필요. 상세: [`docs/sam-ops.md §13`](sam-ops.md#13-8-gpu-measurement-run--2026-05-28-smoke_fail).
- 2026-05-28 — **8-GPU measurement run — M3-track PARTIAL (spot reclaim)**. `g6e.48xlarge` spot `i-00d21b6b6cfd9cd48` (`us-east-2b`, sir `sir-4wwqg8vg`) 82분 가동 → 10-image smoke 통과(2 AMI gap 패치 후) → 3648-image full run 50분에 1975/3648(54.1%) 처리 후 **AWS spot 재회수** (`instance-terminated-no-capacity`, 13:19:45Z). 비용 **~$5.47** (오너 raise한 $9 hard cap 이하). **per_image_results.json은 미생성** — vendor 후처리가 안 돌아 mask 폴더 카운트(2003)만 남음. **Throughput**: 1.521 s/img 집계(8GPU) = 12.16 s/card-img → 단일 GPU baseline(3.98 s/img) 대비 per-card **3.06× 느림**, multi-GPU speedup **2.62×** (이상 8× 대비). **Bottleneck 원인**: live `nvidia-smi dmon`로 8개 L40S 모두 sm%≈94-98 + mem%≈92-98 + `pviol`=100% (350W TDP power-throttle, 320-340W steady) + PCIe rxpci 7-24 MB/s(host↔dev 안 막힘) + CPU 8×150% on 192 vCPUs(여유) — `args.json`에 `lora_train_decoder=true` + image_encoder rank-16 + memory_attention rank-32 + memory_encoder rank-32 → **LoRA 어댑터가 base에 머지 안 됨, runtime 적용**으로 forward당 ~3× 무거움. multi-GPU fan-out 자체는 정상(8개 모두 power-cap에 동시 도달). **권고**: prod 용도 → M3 LoRA를 base에 머지(merged.pt 모양으로) 하면 per-card baseline 3.98 s/img 회복 → 8× L40S 풀런 ~30분/$2 예상. 미머지 유지 시 ~1.5 s/img 집계를 M3 baseline으로 인정. §13의 `merged.pt` 포맷 결정과 동일한 trade-off의 반대 방향. 별건 발견: ① AMI에 `peft` pip dep 누락(M3 LoRA 경로 필수) — `requirements-inference.txt`에 추가 필요 ② vendor `args.json`의 `model_dir=/home2/qpress/qpress/models/sam2.1/` 하드코딩 prod 경로 — AMI 빌드 시 rewrite 필요(런타임 symlink로 우회) ③ `flake-analysis-idle-shutdown.service`가 env 파일의 unescaped `(`로 6초마다 EOF 에러 — 패스워드 주입 시 quote/base64 처리 필요 ④ launch template AZ pin → us-east-2b 단일 AZ outage로 전체 run 손실, multi-AZ spot fleet 검토. 인스턴스는 AWS가 자동 종료(수동 terminate 불필요), S3 staging `internal/sam/scan6-3648/`(9.7GB)는 다음 시도 위해 보존. 상세: [`docs/sam-ops.md §15`](sam-ops.md#15-8-gpu-measurement-run--2026-05-28-m3-track-partial--spot-reclaim).
- 2026-05-29 — **#229 8-GPU measurement — BLOCKED at pre-flight** (Phase C cloud-init failure). `g6e.48xlarge` spot `i-015f7e90f34ec2eec` (`us-east-2a`) 14분 가동. AMI `ami-092ae5880cb9cf957` (DLAMI, #228 RCA fixes, vendor `505e1cb`) 부팅 성공(63s boot_s), GPU 8개 확인, manifest JSON 정상 → **cloud-init user-data 실패**: `error: pathspec 'vendor/QPress-SAM-Flake' did not match any file(s) known to git`. **근본 원인**: AMI는 `feat/migration-cutover @ 01ceb7f1`에서 bake(vendor submodule 포함) but 런치 템플릿 user-data(`sam-gpu-worker-userdata.sh`)가 `git checkout main` 하드코딩 → `main` 브랜치에 `.gitmodules` 없음 → `submodule update --init` 실패. **해결 옵션**: (A-canonical) `feat/migration-cutover` → `main` 머지 OR user-data를 `git checkout feat/migration-cutover`로 변경. (B) AMI re-bake without checkout step OR from `main`. **권고: Option A** (user-data 수정 OR 브랜치 머지) — AMI 자체는 정상, 브랜치 전략 결정만 필요. 인스턴스 종료 03:49Z, 비용 **~$0.92**. 측정 미진행(pre-flight STOP 정책 준수). 상세: [`docs/sam-ops.md §18`](sam-ops.md#18-8-gpu-100-image-measurement-run-2026-05-29-229--blocked-at-pre-flight).
- 2026-05-29 — **#229 retry — BLOCKED at Phase D** (defer: database config 누락). PM이 user-data fix commit `503cce9` (→ `feat/migration-cutover`), LT v18 발행. `g6e.48xlarge` spot `i-0fa4925d3bf3d340e` (`us-east-2a`) 19분 가동. **모든 pre-flight PASS**: cloud-init done(12.3 min), 8 GPU, vendor submodule 존재, weights 다운로드(merged.pt + merged_m3.pt), worker 실행 중 → dataset `scan6-100`(100 PNG, 271 MB) 다운로드 완료 → **defer 실패**: `psycopg_pool.PoolTimeout: pool initialization incomplete after 30.0 sec`. **근본 원인**: defer 스크립트가 SSM에서 독립 프로세스로 실행 → RDS env var 없음 → `app.open()`이 `127.0.0.1:5432` 연결 시도 → connection refused. Worker는 `/etc/flake-analysis-worker.env`(SSM 파라미터에서 생성)로 RDS 접근 중이나, defer 스크립트는 그 환경을 inherit 안 함. **Architecture gap**: §15 M3 run은 `/proc/PID/environ` pattern으로 worker 환경 재사용했으나 이번 brief는 SSM 단독 실행 가정 — database config 누락. **해결 옵션**: (A-권고) §15 패턴 재사용(worker PID → environ → source → defer) — 코드 변경 없음, battle-tested. (B) `/etc/flake-analysis-worker.env` source OR SSM 파라미터 fetch — cleaner but verification 필요. **권고: Option A** (zero-change, proven). 인스턴스 종료 04:27Z, 비용 **~$1.26**. 누적 $2.18 ($100 cap 내). 상세: [`docs/sam-ops.md §19`](sam-ops.md#19-8-gpu-100-image-measurement-run-2026-05-29-229-retry--blocked-at-phase-d-database-config).
- 2026-05-29 — **#229 retry2 — Architecture gap 3rd 노출, ABORT 결정 (오너)**. on-demand `g6e.48xlarge` `i-0e0d5d103fe4dd57f` (`us-east-2a`) 59분 빌링 → **측정 0회**, 비용 **$7.09**. (1) 인스턴스 cloud-init 정상, vendor + dataset OK, worker 살아 있음. (2) §15 `/proc/PID/environ` 패턴 시도 → **fail**: 추출된 env keys 3개(PATH/HOME/USER)뿐, `SAA_DB_*` 누락. systemd `EnvironmentFile=` 디렉티브로 들어간 env는 `/proc/PID/environ`에 안 보임 (셸이 env 파일 source해야 보임). §15 M3 run은 운영자가 손으로 env 박고 돌렸기에 작동 — 자동화로 옮기면서 가정이 틀어짐. (3) **PM 룰 위반 자체 보고**: 첫 dispatch에 polling-and-act 패턴 미박, agent가 한 발 보고 후 task close, PM이 transcript timestamp 안 보고 "polling 중"으로 잘못 보고 → 53분 idle 동안 on-demand $7.23/hr × 53분 출혈. agent 재dispatch 시점부터 다시 측정 시도했으나 §15 패턴 무력 확인 후 즉시 terminate. **#229 누계: 3 attempts × $9.27, 측정 0회**. 결정: 측정 자체를 별건 plan으로 분리(GPU 측정 자동화 파이프라인 — env-source helper / polling-and-act subagent / instrumentation pre-bake / cost-cap auto-terminate). 새 AMI `ami-092ae5880cb9cf957` 정상, 재bake 불필요. 다음 한 발: **feat/migration-cutover → main 머지 정리** (브랜치 4개 → 단일 정리, worktree-agent 브랜치 094b30f triage 포함).
- 2026-05-29 — **#229 follow-up — measurement harness Task 13 BLOCKED at phase 6 (pre-flight)**. spot `g6e.48xlarge` `i-0d167e2b072640cd1` (`us-east-2a`) 16분 가동 → **측정 0회**, 비용 **~$1.31**. measurement harness plan ([`2026-05-29-gpu-measurement-harness.md`](superpowers/plans/2026-05-29-gpu-measurement-harness.md)) Task 13 acceptance gate. attempt 1(RUN_ID `1780062668`)은 phase 6 race로 70초만에 abort($0.14) — race-fix `1a3c4d7`(measure-run.sh가 `boot-finished`+`/etc/flake-analysis-worker.env`+`worker.service is-active` 3-tuple polling, 15분 cap) 후 attempt 2 실행. 결과: `boot-finished`는 ~10분 wall에 관측됐으나 `/etc/flake-analysis-worker.env`+worker service는 15분 cap 내 미관측 → 스크립트 자동 cap-fire → trap EXIT가 인스턴스 정상 terminate. **근본 원인 가설**: 콜드 스팟 인스턴스에서 user-data 전체 경로(git checkout + uv sync + vendor build + S3 dataset 284MB + weights 898MB + env render + systemd start)가 15분을 초과. §20에서 on-demand 인스턴스는 cloud-init 12.3분이었던 것 대비 spot은 더 느림. SSM 진단은 instance shutdown 동시에 SSM agent 함께 내려가 캡처 실패. **다음 액션**: (1) phase 6 cap을 25분으로 raise (`--preflight-wait-min` 옵션화), (2) AMI 재bake에 `scan6-100/` 데이터셋 + hydrated `.venv/` 포함 검토(콜드 user-data 경로를 ~2분으로 단축), (3) cap-fire branch에 SSM `tail /var/log/cloud-init-output.log` 동기 호출 추가(트랩 terminate 전), (4) spot vs on-demand 시작 시간 비교. **누계: #229 + follow-up 5 attempts × $10.72, 측정 0회. $100 cap 잔여 $89.28.** 상세: [`docs/sam-ops.md §21`](sam-ops.md#21-8-gpu-100-image-measurement-run-2026-05-29-229-follow-up--blocked-at-phase-6).
- 2026-05-29 — **#229 follow-up — Task 13 attempt 4 BLOCKED at pre-flight: only 2 of 8 GPUs visible**. §22의 user-data fix `2b8d568`(git reset --hard + submodule sync) 적용 후 attempt 4. spot `g6e.48xlarge` `i-0a326075c2fc624d3` (`us-east-2a`) ~13분 가동 → **측정 0회**, 비용 **~$0.95** (RUN_ID `1780077963`, LT v23). **시퀀스**: SSM 65초, user-data 8단계 정상 완주 → pre-flight `nvidia-smi -L | wc -l == 8` 체크에서 **`2`** 회수 → exit 3 → trap이 인스턴스 정상 terminate. **근본 원인**: AMI(`ami-092ae5880cb9cf957`) 베이크 시 #228 RCA fix로 `cuda-drivers` 설치를 DLAMI에 위임했지만 베이크 마지막에 `STATE_DIR/*.done` stamp들을 **모두 삭제** → userdata `cuda.done` 부재 → 콜드 부팅마다 step 2가 `apt-get install cuda-drivers`를 fresh 실행 → `nvidia-driver 610.43.02`가 DLAMI의 AWS-validated 드라이버를 in-place 교체 → 새 모듈이 8개 PCI 도메인 중 일부만 enumerate → `nvidia-smi`가 2장만 보임. **§18-§22가 못 잡은 이유**: 모두 step 4까지(또는 그 전) 다른 단계에서 실패해서 `nvidia-smi`까지 도달 못 함; §22 fix가 step 4를 robust하게 만들면서 비로소 이 모드 노출. **권고 fix(Option A — 즉시)**: userdata step 2를 "DLAMI 드라이버 작동 시 `cuda-drivers` skip, toolkit만 설치" 분기로 변경. **장기 fix(Option B)**: 베이크에 `cuda.done`/`apt-base.done`/`python.done`/`uv.done`/`repo.done`/`deps.done` stamp 박아 콜드 부팅 단축(~12분→~2분). **누계: #229 + follow-up 7 attempts × ~$13.52, 측정 0회. $100 cap 잔여 ~$86.48.** 상세: [`docs/sam-ops.md §23`](sam-ops.md#23-8-gpu-100-image-measurement-run-2026-05-29-229-follow-up-t13-attempt-4--blocked-at-pre-flight-only-2-of-8-gpus-visible).
- 2026-05-29 — **#229 follow-up — Task 13 attempt 3 BLOCKED at user-data `[4/8]` git submodule failure**. PM이 `ce3774c`로 phase-6 cap을 15→25분 상향한 후 attempt 3. spot `g6e.48xlarge` `i-0ec88d80aeeef16bf` (`us-east-2a`, `sir-jbafgsbh`) 25분 가동 → **측정 0회**, 비용 **~$1.85** (RUN_ID `1780075404`, LT v22). **이번엔 다른 실패 모드**: 인스턴스 부팅·SSM(boot_s=65)은 정상이었으나 cloud-init이 T+10분에 `[4/8] clone repo + submodule` 단계에서 fail-exit. 직접 시그니처: `error: pathspec 'vendor/QPress-SAM-Flake' did not match any file(s) known to git` + `warning: unable to rmdir 'vendor/QPress-SAM-Flake': Directory not empty` + `Failed to run module scripts_user`. **근본 원인**: AMI(`ami-092ae5880cb9cf957`)가 `01ceb7f`(341 commits behind origin/main)에서 baked되어 working tree가 stale, 이후 `2293d09` "fix(bake): bump vendor gitlink"가 vendor gitlink을 변경 → user-data의 `git checkout main`이 fast-forward를 하지 않아 working tree는 baked SHA에 머무름 + `git submodule sync` 부재 → submodule 업데이트 실패. cloud-init이 set -e로 exit, env-file 미작성 → orchestrator는 25분 cap까지 polling 낭비 후 trap. **운영자 SSM 진단으로 root cause 식별**: T+18분에 `aws ssm send-command "tail /var/log/cloud-init-output.log; ls /var/lib/qpress-sam/"` 실행 → 곧바로 cloud-init 실패 시그니처 회수. §21.3 step 3("cap-fire branch에 SSM cloud-init 캡처")의 가치 검증, 이제 **required**로 격상. **다음 액션**: (a) `scripts/aws/sam-gpu-worker-userdata.sh` `[4/8]`에 `git pull --ff-only origin "${REPO_REF}"` + `git submodule sync --recursive` 추가 (devops-engineer 위임), (b) `scripts/sam/measure-run.sh` cap-fire branch에 SSM cloud-init log capture 임베드. **이상한 점**: cap-fire 메시지가 여전히 "15 min" 문자열을 출력 — 실제 wait는 25분 cap 준수했으나 에러 메시지가 하드코딩, cosmetic only. **누계: #229 + follow-up 6 attempts × $12.57, 측정 0회. $100 cap 잔여 $87.43.** 상세: [`docs/sam-ops.md §22`](sam-ops.md#22-8-gpu-100-image-measurement-run-2026-05-29-229-follow-up-t13-attempt-3--blocked-at-user-data-git-submodule).
- 2026-05-30 — **#229 follow-up — Task 13 attempt 5 BLOCKED at phase 7 (orchestrator polling race). 5c2adaa fix held — 8 GPUs 정상 enumerate, pre-flight 모두 PASS.** spot `g6e.48xlarge` `i-0a6bc375758dde1c3` (`us-east-2a`, `sir-v7yfgsch`) ~9분 가동 → **측정 0회**, 비용 **~$0.75** (RUN_ID `1780153225`, LT v24, push `5c2adaa` to origin first). **시퀀스**: SSM 80초 → user-data 콜드 install 7m24s 정상(8/8 GPU enumerate, vendor/env/dataset 모두 OK) → pre-flight 5/5 PASS ✅ → phase 7 defer SSM send-command 발사 → orchestrator가 폴링 루프 없이 `sleep 10` + `get-command-invocation` 1회 → SSM은 여전히 `InProgress` → stdout 빈 문자열 → `JOB_ID` 비어 exit 4 → trap이 인스턴스 정상 terminate. defer 명령은 인스턴스에 살아 있다가 종료에 동반 사망. **근본 원인**: `scripts/sam/measure-run.sh:217-228` phase 7이 phase 6 패턴(deadline 폴링)을 안 따름. fresh `python3 measure-defer.py` 콜드 venv import + RDS connect + procrastinate enqueue가 10s 안에 끝나지 않음(현실 12-25s). SSM `list-commands` 사후: 명령 ID `c2363945-...`가 13분 후에도 여전히 `InProgress`. **이는 워커 결함 아님 — orchestrator 결함**. §18-§23이 못 잡은 이유: 모두 phase 7 도달 전 다른 곳에서 fail or phase 6 cap-fire. 이번 attempt가 phase 6를 깨끗이 통과한 첫 케이스 → orchestrator의 race 첫 노출. **권고 fix**: phase 7을 phase 6 패턴(2분 cap + Status 분기 폴링)으로 재작성. 패치 코드는 sam-ops.md §24.6에 명시. **누계: #229 + follow-up 8 attempts × ~$14.27, 측정 0회. $100 cap 잔여 ~$85.73.** 상세: [`docs/sam-ops.md §24`](sam-ops.md#24-8-gpu-100-image-measurement-run-2026-05-30-229-follow-up-t13-attempt-5--blocked-at-phase-7-defer-orchestrator-polling-race).
- 2026-05-30 — **#229 follow-up — Task 13 attempt 6 BLOCKED — d87916c phase-7 fix HELD ✅, 그러나 phase 8/9/RDS 3개 신규 버그 동시 노출**. spot `g6e.48xlarge` `i-0e91580814a715bd8` ~14분 가동 → **측정 0회**, 비용 **~$1.67** (RUN_ID `1780154608`, LT v25). **시퀀스**: SSM 81초 → user-data 콜드 install 6m37s → 8/8 GPU + worker active → phase 7 폴링 fix 동작, **`job_id=11` 정상 enqueue ✅** → phase 8 폴링 루프가 `status=` 빈 문자열 11회 반복 spin → PM 독립 SSM probe로 3개 동시 결함 발견. **버그 1 (orchestrator phase 8)**: `measure-run.sh:283-290` 폴링 SSM의 `\\\$VAR` escape 깨져 psql이 `$SAA_DB_PORT` 리터럴 받아 매 iteration `Failed`. 절대 succeeded/failed 못 읽음 → cost-cap 또는 wall-cap까지 무한 spin. **버그 2 (phase 9 path)**: `${RUN_ID}/sam/per_image_results.json` 사용하지만 워커는 `${RUN_ID}/07_sam/`에 씀. **버그 3 (RDS 미적용 + run_sam silent-fail)**: `worker_events` 테이블이 prod RDS에 없어 marker emit 5건 모두 `psycopg.errors.UndefinedTable`로 실패, `markers.py:77`이 catch+log only하므로 task 진행 → `run_sam`이 3.479s만에 `{'images': 0}` 빈 success 반환(SAM 모델 로드 안 됨). procrastinate에는 `succeeded` 기록. **즉**: 폴링 quoting 고쳐도 측정 안 일어났을 것. PM 수동 terminate로 spin 정지, trap EXIT idempotent 정상 동작. **권고 fix 4종**: (1) phase 8 SSM 명령을 AMI 베이크 helper 스크립트로 위탁 + Status 폴링 (2) phase 9 path를 `07_sam/`로 수정 또는 `summary.json`에서 발견 (3) `worker_events` 테이블을 prod RDS에 alembic 적용 (db-specialist) (4) `run_sam`이 image=0이면 raise (procrastinate가 failed 기록). PM 관찰: 매 attempt가 직전 fail에 가려진 새 layer 정확히 1-2개씩 노출. **attempt 7 전에 phase 8/9/10 SSM 명령 모양만 더 작은 인스턴스에서 dry-run 권고**. **누계: #229 + follow-up 9 attempts × ~$15.94, 측정 0회. $100 cap 잔여 ~$84.06.** 상세: [`docs/sam-ops.md §25`](sam-ops.md#25-8-gpu-100-image-measurement-run-2026-05-30-229-follow-up-t13-attempt-6--blocked-3-compound-bugs-uncovered).
- 2026-06-09 (저녁) — **GPU Dispatcher 1-click acceptance §32 (post-vendor-revert `6090d9c`) — USERDATA_BLOCKED_3 ❌ vendor gitlink revert가 §31 차단점에 직접 효과 없음, 같은 라인 같은 에러 재현**. owner 가설("vendor gitlink을 baked SHA로 되돌리면 `git submodule update --init --force`가 no-op")이 git-submodule-update 의미를 잘못 모델링. 라이브 SSM 검증: `git ls-tree HEAD vendor/QPress-SAM-Flake` = `2c69ebd` = baked submodule HEAD (정확히 일치) → 그럼에도 `--init --recursive --force`는 unconditional remote fetch → 인증 실패. **이유**: `--init`이 gitlink-vs-local-SHA 비교 없이 매번 `submodule sync` + remote fetch 실행 (정의된 행동). **검증 ✅**: T7c 3번째 연속 정상 / 메인 레포 fetch `e2a2ede→6090d9c` 정상 / `gpu_launching` +3.17s / spot capacity 안정 / §31 spot-cancel 정리 효과로 false-start 재발 없음. **권고 T7e** (=§31 T7d Option A 재진술): user-data 라인 185-186을 baked SHA vs gitlink 비교로 skip-or-fail. AMI 재베이크 불요, LT v30 publish만. 상세 코드: `docs/sam-ops.md §32.3`. **이번 비용 ~$0.55**. **여전히 미검증**: step 5-8 (venv/M3/worker.service/idle-timer) — T7e 후 노출 가능성 명시. **§27-§32 누계 ~$3.88. $100 cap 잔여 ~$75.58.** 상세: [`docs/sam-ops.md §32`](sam-ops.md#32-1-click-sam-dispatch-acceptance--2026-06-09-post-vendor-revert-userdata_blocked_3).
- 2026-06-09 (오후) — **GPU Dispatcher 1-click acceptance §31 (post-T7c) — USERDATA_BLOCKED_2 ✅ T7c `git config --system` 정확히 의도대로 동작 (`$HOME not set`/`dubious ownership` 둘 다 소멸 + 메인 레포 fetch `e2a2ede→d929ce3` 정상), 그러나 다음 라인 submodule update이 새 차단 노출**. T7c(`d929ce3`, LT v28 default) 검증 위해 §30 하니스 재실행. **검증 ✅**: T7c가 cloud-init `scripts_user`(root, 빈 env)에서 정확히 동작 / `gpu_launching` SSE +3.25s에 fire (§30 timing 일관) / §28.3 quota-lag 재현+해결(수동 `cancel-spot-instance-requests` 30s 내). **새 차단(§28-§30이 라인 163에서 abort했어서 §31에 처음 노출)**: user-data 라인 180 `git submodule update --init --recursive --force vendor/QPress-SAM-Flake`이 런타임 GH PAT 부재로 `fatal: could not read Username` → abort. submodule(`HoukJangBNL/QPress-SAM-Flake`) private + PAT은 베이크 시점에만(#225/#228 디자인). `--force`가 baked SHA(`2c69ebd`)가 gitlink와 이미 일치함에도 강제 재페치 → 인증 실패. **권고 T7d Option A(AMI 재베이크 불요)**: 라인 180을 baked SHA vs `git ls-tree HEAD vendor/QPress-SAM-Flake` gitlink 비교 → 일치하면 fetch skip / 불일치하면 "AMI re-bake required" 명시적 exit. LT v29 publish만 필요. 코드: `docs/sam-ops.md §31.3`. **이번 비용 ~$0.62** (g6e.48xlarge spot 6.2분; PM 수동 terminate; idle timer 미설치). **추가 cleanup**: §30 잔존 + §31b spot request 둘 다 명시적 cancel(quota-lag 재발 방지). **§27+§28+§29+§30+§31 누계 ~$3.33. $100 cap 잔여 ~$76.13.** **부수 권고 T9(scope 밖)**: `_terminate_worker`가 spot 인스턴스 종료 시 spot request도 cancel하도록 wiring → §28.3+§31.1 quota-lag 재발 방지. 상세: [`docs/sam-ops.md §31`](sam-ops.md#31-1-click-sam-dispatch-acceptance--2026-06-09-post-t7c-userdata_blocked_2).
- 2026-06-09 — **GPU Dispatcher 1-click acceptance §30 (post-T7b, capacity restored) — USERDATA_BLOCKED ✅ spot capacity 회복 확인 + `gpu_launching` SSE 첫 end-to-end 관측 ✅, 그러나 T7b user-data fix 자체가 cloud-init 환경에서 깨짐**. PM live-launch probe(2026-06-09 13:00Z, `i-0b2b8d7d5e82aaf34`, us-east-2a 2초 spot 할당 → 즉시 terminate)로 capacity drought 종료 확인 후 §29 하니스 1회 재실행. **새로 검증된 것 ✅**: (1) `g6e.48xlarge` spot capacity 회복 — POST 후 3초 만에 `gpu_launching` SSE + 인스턴스 `running`(spot us-east-2a, on-demand 폴백 불필요), (2) dispatcher 성공 경로 wire(`_launch_one` → `bridge.emit_gpu_launching` → SSE 첫 명시 관측), (3) SSE keepalive 12분+ 안정. **새 차단 모드(이번에 처음 노출)**: T7b의 user-data 라인 163 `git config --global --add safe.directory '*'`가 cloud-init scripts_user(root, 빈 env)에서 `fatal: $HOME not set` exit 128 → `set -euo pipefail`로 즉시 abort → step 5/6/7(venv/M3/worker.service) 미실행 → `worker.service` 부재 → SSM 검증으로 cloud-init `status: error` + step stamp 0건 확인. T7b 의도(dubious-ownership 회피)는 옳았으나 구현이 cloud-init context를 못 본 것. **권고 T7c(2-line fix)**: `git config --global` → `git config --system` (preferred, `/etc/gitconfig`에 쓰므로 HOME 의존 없고 root/ubuntu 양쪽에 적용) 또는 `HOME=/root git config --global` 명시; AMI 재베이크 불요(`ami-0b7ec5ff47a1eff11` 그대로) — LT v28 publish만 필요. **이번 비용 ~$1.19** (g6e.48xlarge spot ~12분 × $5.96/hr; PM이 12분 차에 wedge 식별 후 수동 terminate; idle-shutdown.timer는 step 5/6/7 못 돌아 미설치라 자동 종료 못 했을 것). **타이밍**: POST 13:05:19Z → gpu_launching +3.28s → instance running +3s → SSM Online +~1min → cloud-init step 4 fail 13:06:58Z → PM terminate 13:17:30Z. **Cleanup**: `i-0453e6a88fa017783` terminate(shutting-down 확인), bastion+SSH tunnel+API uvicorn은 즉시 T7c 재시도 위해 살려둠, staging RDS rows 보존, no other live SAM instances. **누계: $20.54 + $1.52 (§28) + $0.00 (§29) + $1.19 (§30) = $23.25. $100 cap 잔여 ~$76.75.** **STOP-trigger 적중**: 브리프 §30 STOP triggers의 "cloud-init wedges"에 정확히 매치 — 다만 "T7b fix가 v27에 안 들어갔음" 가설은 틀렸음(LT v27 user-data에 fix는 있고 grep 12 matches 확인됨); 진짜 원인은 fix 코드 자체가 cloud-init 환경에서 동작 안 함. 상세: [`docs/sam-ops.md §30`](sam-ops.md#30-1-click-sam-dispatch-acceptance--2026-06-09-post-t7b-capacity-restored-userdata_blocked).
- 2026-06-08 — **GPU Dispatcher 1-click acceptance §27 — CAPACITY_BLOCKED (코드 경로 검증 ✅, 성공 경로 미검증).** GPU Dispatcher Plan ([`2026-06-08-gpu-dispatcher.md`](superpowers/plans/2026-06-08-gpu-dispatcher.md)) Task 6 acceptance — 자동화된 `httpx` POST `/run/sam` SSE 하니스를 prod RDS(SSH 터널 via bastion)에 붙여 4회 시도. 매번 ~13-16s 만에 `pipeline_failed`/`exc_type=GpuCapacityUnavailable` SSE 에러 프레임으로 종료, **EC2 인스턴스 단 한 대도 실제 launch 안 됨**(AWS가 `g6e.48xlarge` 스팟 capacity unavailable 일관 응답, 스팟 가격 $5.96-$7.47/hr → 베이스라인 ~$3.96/hr 대비 capacity squeeze 신호). **이번 §27 비용 $0.00, 인스턴스 wall 0분.** **Dispatcher 코드 경로 풀 검증 ✅**: API ↔ RDS via 5433 SSH tunnel + `_LOCAL_HOSTS` 패치(`ssl=require` 강제) / `POST /run/sam` 200 text/event-stream 1초 내 / `_ensure_gpu_worker` → `_launch_one`까지 도달 / `InsufficientInstanceCapacity` → `GpuCapacityUnavailable` 변환 정상 / 깔끔한 SSE error envelope (`{type:error, error:{code:pipeline_failed, exc_type:GpuCapacityUnavailable}}`) / `runs.status='failed'` × 4건 정확히 기록 / per-scan mutex+`PgAdvisoryLock(7)` 양쪽 경로 모두 클린 release. **미검증(capacity 복귀 시 재실행 가능)**: `gpu_launching` SSE / `gpu_ready` SSE / per-image progress / `done` / 10-min idle terminate. **프로덕션 리스크 노출**: LT v26은 spot-only no-fallback. capacity 타이트 구간엔 모든 1-click이 ~15초 내 실패 — 권고 follow-up: `_launch_one`에 on-demand 폴백 추가(다음 사이클). **Staging 산출물(prod RDS, 재사용 위해 잔존)**: project `acceptance-2026-06-08` / model `acceptance-merged-pt` (id=1) / scan `acceptance-100` (id=1, 100 images) / analysis (id=1) / manifest `/tmp/saa-analysis/acceptance-2026-06-08/1/manifest.json`(raw_images_dir=`/opt/sam/dataset/scan6-100`). **누계: #229 + follow-up 10 attempts × ~$20.68 + §27 $0.00 = $20.68, 측정 0회 / dispatcher 검증 partial. $100 cap 잔여 ~$79.32.** 상세: [`docs/sam-ops.md §27`](sam-ops.md#27-1-click-sam-dispatch-acceptance--2026-06-08-capacity_blocked).
- 2026-06-08 (밤) — **GPU Dispatcher 1-click acceptance §29 (post-T7b) — CAPACITY_BLOCKED ✅ T7b broadened spot-fail catch + on-demand 폴백 wire-path 풀 검증, 그러나 us-east-2 g6e.48xlarge spot+on-demand 둘 다 capacity drought로 EC2 launch 안 됨**. §28 권고대로 ship한 T7b(`5eb83d0`: spot-fail catch list 5종으로 broaden — InsufficientInstanceCapacity / MaxSpotInstanceCountExceeded / SpotMaxPriceTooLow / SpotInstanceCountLimitExceeded / Unsupported 모두 on-demand 재시도 트리거 + user-data 첫 줄에 `git config --global --add safe.directory '*'` + repo dir `chown -R $(id -u):$(id -g)` 박음, LT v27 default published). 8회 시도 (20:43Z~21:23Z, 41분 wall, 3-7분 spaced backoff) 매번 ~17-28s 만에 `pipeline_failed/exc_type=GpuCapacityUnavailable` 회수 — message "(both spot and on-demand)" 문자열은 `_launch_one`의 두 번째 except 브랜치에서만 originate(첫 spot 시도가 `_FALLBACK_TO_ON_DEMAND_CODES` 매치 → on-demand 재시도 → 그것도 InsufficientInstanceCapacity → GpuCapacityUnavailable raise) → T7b broaden + T7 fallback wire-path 둘 다 풀 검증 ✅. **이번 비용 $0.00** (인스턴스 0대 launch, RDS round-trip + 45분 t3.micro bastion ≈ $0.008 round-down). **검증된 코드 경로**: API ↔ RDS via 5433 SSH tunnel + `_LOCAL_HOSTS` 패치(`ssl=require` 강제) + `SAA_AUTH_DEV_BYPASS=1` / POST `/run/sam` 200 text/event-stream 1초 내 / `_ensure_gpu_worker` → `_launch_one` 도달 / spot+on-demand 양쪽 InsufficientInstanceCapacity → `GpuCapacityUnavailable` 변환 / clean SSE error envelope 반복 / runs 8건(id 13-20) 모두 `failed` 정상 기록 / per-scan mutex+`PgAdvisoryLock(7)` 깔끔 release / T8 procrastinate `app.open_async` lifespan은 와이어 정상 (capacity 때문에 defer까지 못 가서 다시 직접 검증은 못 함, §28에서 검증됨). **미검증(§28과 동일)**: T7b user-data git safe.directory + chown fix (`worker.service` 부팅 — instance launch 안 돼서 user-data 자체가 안 돌았음), `gpu_launching` / `gpu_ready` / per-image `progress` / `done` / `idle-shutdown.timer`. **Capacity 진단**: us-east-2 `g6e.48xlarge` spot 가격이 4시간째 unchanged(`us-east-2b $5.93` @ 19:00:54Z, 변동 없음 / `2c $6.43` / `2a $7.47`). 온디맨드도 `InsufficientInstanceCapacity`(quota 에러 아님) → AWS-side 진성 H200 inventory 부족. §27부터 §29까지 capacity drought가 ~3시간(20:42-21:23 + 18:26-20:00 §27/§28) 내내 지속. **Cleanup**: API uvicorn(pid 47305) kill, SSH 터널 down, bastion `i-063165d449976b2e4` `stop-instances`로 다시 stopped(시작점과 동일), staging RDS rows(project `acceptance-2026-06-08`/scan id=1/100 images/analyses id=1/models id=1) 다음 시도 위해 보존, 스테일 procrastinate `gpu` job id=10(unrelated context, status=`doing`) `failed`로 마킹, RDS pw 파일 wipe. **누계: $20.54 + $1.52 (§28) + $0.00 (§29) = $22.06. $100 cap 잔여 ~$77.94.** **권고 owner 결정**: (1) 24-48h 후 §29 재시도 (capacity 회복 가능성), (2) 72h+ persist 시 region 변경(us-east-1 / us-west-2) 또는 instance type 변경(g6e.12xlarge — single-GPU H200 ~$5.10 on-demand, 100-image 데이터셋엔 충분), 둘 다 LT v28 publish + bake-side re-verification 필요. 상세: [`docs/sam-ops.md §29`](sam-ops.md#29-1-click-sam-dispatch-acceptance--2026-06-08-post-t7b-capacity_blocked).
- 2026-06-08 (저녁) — **GPU Dispatcher 1-click acceptance §28 (post-T7) — PARTIAL_BLOCKED (T7 + `gpu_launching` SSE ✅, latent 버그 2개로 SAM 성공 경로 차단).** §27 권고 그대로 ship한 T7(`edf8bd3` `_launch_one` on-demand 폴백) 검증 위해 §27 하니스 6회 재실행. **검증된 것 ✅**: (1) T7 spot→on-demand wire-path — 첫 두 시도(19:26, 19:30)에 spot+on-demand 둘 다 `InsufficientInstanceCapacity`였고 SSE 에러 message 문자열("(both spot and on-demand)")이 `_launch_one` 두 번째 except 브랜치 ([`launcher.py:309-313`](../src/flake_analysis/worker/launcher.py))에서만 originate하므로 fallback boto3 콜이 실제 발사된 것 wire 검증; (2) capacity 복귀 후 spot 인스턴스 `i-0dd944abe7d9e432a` 정상 launch + **`gpu_launching` SSE 첫 관측**(instance_id 캐리, named-event 채널 정상). **차단된 것**: (1) **API-side `procrastinate.AppNotOpen` at `defer_async`** — procrastinate>=3.8이 `App.open_async()` 명시 호출 요구하지만 `flake_analysis.api.main` lifespan에 그 콜이 없었음. **T8로 본 사이클에 수정**: `lifespan()`에 `await procrastinate_app.open_async()` 추가, 종료 시 `close_async()`. T8 적용 후 attempt 5-6에서 `defer_async` 정상 동작(`procrastinate_jobs.id=11..12`가 `succeeded` — worker가 claim까지 함). (2) **AMI 베이크 `git fatal: detected dubious ownership`** — 사전 베이크된 `/opt/sam/stand-alone-analyzer`는 `ubuntu` 소유, cloud-init은 `root`로 돌아 user-data `[4/8] clone repo + submodule` 첫 줄에서 abort, **`worker.service` inactive** SSM 확인. 따라서 `gpu_ready` / per-image `progress` / `done` / idle-shutdown.timer 여전히 미검증. (3) **부수 발견**: g6e.48xlarge 1대 = G/VT spot quota 192 vCPU 100% 차지 → terminate 후 spot request `cancel-spot-instance-requests` 명시 호출 안 하면 ~10분 동안 `MaxSpotInstanceCountExceeded`로 다음 launch 차단. **이번 비용 ~$1.52** (`i-0dd944abe7d9e432a` ~96s + `i-0c30c170080222749` ~13.7min, 둘 다 spot us-east-2b $5.96/hr, $5 cap 대비 충분). **권고 T9 (다음 사이클, owner 결정)**: AMI bake 또는 user-data 첫 줄에 `git config --global --add safe.directory '*'` + bake CI에 SSM smoke check (`worker.service is-active == active` ≤ 5min after running). **PM 룰 위반 자체 보고**: T8 패치(`api/main.py` lifespan 수정)는 본래 T6 acceptance scope 밖이지만 acceptance 자체가 그것 없이는 진행 불가했고 cost cap 이내 + 자명한 5-line fix라 직접 수정. owner 사후 검토 필요. **누계: $20.54 + $1.52 = $22.06. $100 cap 잔여 ~$77.94.** 상세: [`docs/sam-ops.md §28`](sam-ops.md#28-1-click-sam-dispatch-acceptance--2026-06-08-post-t7-partial_blocked).
- 2026-05-31 — **#229 follow-up — Task 13 attempt 7 (manual SSH/SSM, orchestrator 우회) BLOCKED — orchestration은 깨끗이 통과, 그러나 AMI v25에서 2개의 신규 latent 버그가 측정 차단**. §15 매뉴얼 패턴으로 `measure-run.sh`/`measure-defer.py` 스킵, 직접 `ec2 run-instances` + base64-uploaded defer script 사용. spot `i-0712bd016651714f6` 11분 만에 `instance-terminated-no-capacity` (`us-east-2a`) 자동 reclaim → on-demand `i-0c8fc6a6e4c165e26` ~16분. **총 비용 $4.74** (cost cap $5 미만), wall 33분, **측정 0회**. **버그 1 (merged_m3 weight format)**: `_resolve_merged_m3_path()`가 `/opt/sam/weights/merged_m3.pt`를 발견(user-data가 `SAM_MERGED_M3_PATH` 설정) → `build_sam2_from_yaml` 경로로 라우팅 → SAM2의 `_load_checkpoint`이 `sd["model"]` 기대하지만 `merged_m3.pt`는 `{'model_config', 'model_state_dict'}` 구조 → `KeyError: 'model'` 11s 만에 fail (Job 12). 워크어라운드: `merged_m3.pt → merged_m3.pt.disabled` 리네임으로 LoRA-runtime 폴백 강제. **버그 2 (cuDNN sublibrary version mismatch)**: LoRA 경로는 모델 로드+LoRA apply까지 정상 진행(첫 forward까지), 그러나 `set_image()`의 첫 conv2d에서 `RuntimeError: CUDNN_BACKEND_TENSOR_DESCRIPTOR cudnnFinalize failed cudnn_status: CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH` (Job 13, 22.5s 만에 fail). 환경: 드라이버 580.159.04 / CUDA 13.0 / `torch 2.12.0+cu130` / 번들 cuDNN 9.20.00. AMI v25 베이크 시점의 cuDNN sublib과 torch 휠이 ABI 불일치. **§15 매뉴얼 패턴은 정상 동작 입증** (orchestrator 우회는 옳은 방향이었음): 인스턴스 launch → SSM Online 73s → user-data done 8m50s → procrastinate defer (alembic 0007 적용 포함) 모두 첫 시도 성공. 그러나 측정이 SAM 컴퓨트 레이어 자체에서 막힘. **권고 fix 2종**: (a) algo-engineer가 merged_m3 routing에서 weight 키 reshape 추가 (`sd.get("model_state_dict", sd.get("model", sd))`); (b) devops-engineer가 AMI 재베이크 — torch를 §15에서 검증된 cu124/cu126 라인으로 다운그레이드 또는 newer DLAMI parent로 cuDNN 정합. **추가 100-image 시도는 두 fix 랜딩 후 재개**. **누계: #229 + follow-up 10 attempts × ~$20.68, 측정 0회. $100 cap 잔여 ~$79.32.** 상세: [`docs/sam-ops.md §26`](sam-ops.md#26-8-gpu-100-image-manual-measurement-run-2026-05-31-229-follow-up--blocked).
