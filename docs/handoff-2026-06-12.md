# Session Handoff — 2026-06-12

> **Purpose**: 다음 세션이 GPU Dispatcher 1-click acceptance가 끝난 직후 상태에서 즉시 따라잡고, post-SUCCESS hardening 작업으로 진입할 수 있게.

---

## 1. 한 줄 요약

**🎉 GPU Dispatcher 1-click 100-image SAM acceptance SUCCESS.** 16 retries, 14 fixes (T7c–T7q) 끝에 §43에서 g6e.48xlarge 8-GPU spot us-east-2b로 100장 / 1405 masks / 0 errors / $0.84. cumulative ~$9.79 / $100. Spec/Plan acceptance gate closed.

---

## 2. 현재 상태 (2026-06-12 EOD)

### 2.1 SUCCESS 결과 (§43, 2026-06-12 12:52 UTC)

| Field | Value |
|---|---|
| Branch / HEAD | main / `ccbf6c7` |
| Instance | `i-0ddf469a871e8dae9` — `g6e.48xlarge` spot us-east-2b |
| AMI | `ami-0b7ec5ff47a1eff11` (cu124, §15-verified) |
| LT | `qpress-sam-gpu-worker` v37 default |
| Run / Job | runs id=37 status=`completed`, procrastinate id=27 status=`succeeded` |
| Images | 100/100 ✅ |
| Masks total | 1405 |
| Errors | 0 |
| Inference wall | 228 s (~2.28 s/img on 8× L40S) |
| Cost | $0.84 |

### 2.2 누적 fix (T7c–T7q)

| Fix | Commit | 설명 |
|---|---|---|
| T7c | `d929ce3` | `git config --system` (cloud-init `$HOME` 부재) |
| T7e | `2f56bce` | vendor SHA-equality skip (PAT not at runtime) |
| T7g | `19cd876` | `git submodule sync` + per-call `recurseSubmodules=no` |
| T7h | `c7e2647` | vendor gitlink ↔ AMI baked SHA align (`2c69ebd`) |
| T7i | `9011618` | idle timeout 10→30 min (diagnostic window) |
| T7j-A | `09b5908` | DB pw from Secrets Manager (RDS auto-rotate) + IAM |
| T7l | `d6efe94` | `merged_m3.pt` → `.disabled` (LoRA-runtime path force) |
| T7m | `b068036` | `deps.done` invalidate (peft install at cold boot) |
| T7n | `f95c96f` | multi-AZ rotation (3 AZ × spot+ondemand = 6) |
| T7o | LT v37 | `g6e.12xlarge` default (capacity 풍부) |
| T7p | `d654ace` | completed SSE slim payload (NOTIFY 8KB cap) |
| T7q | `d654ace` | instance-type ladder (8/4/4/1 GPU, 4 tiers) |

### 2.3 인프라 상태

| | |
|---|---|
| AMI | `ami-0b7ec5ff47a1eff11` (cu124 stack, peft baked, vendor `2c69ebd`) |
| LT | `qpress-sam-gpu-worker` v37 default (g6e.12xlarge, ami-0b7ec5ff47a1eff11) |
| IAM | `qpress-sam-gpu-role` + inline `qpress-sam-gpu-secret-rw` (Secrets Manager scoped) |
| Subnets | 3 AZs (us-east-2a/b/c) — code rotates via T7n |
| Live instances | 0 (verified 2026-06-12 EOD) |
| Working tree | clean |

### 2.4 Cumulative cost (T6 acceptance only, §27–§43)

- §27–§42 wasted launches: ~$8.95
- §43 SUCCESS run: $0.84
- **Total**: ~$9.79 of $100 cap

---

## 3. Spec/Plan acceptance gate

`docs/superpowers/specs/2026-06-08-gpu-dispatcher-design.md` §3 architecture + `docs/superpowers/plans/2026-06-08-gpu-dispatcher.md` Task 6 (live acceptance smoke) = **PASSED 2026-06-12**.

Every line of the §27 dispatcher contract verified end-to-end:
```
POST /run/sam → API gate → procrastinate defer → _launch_one (T7q ladder)
  → instance running → cloud-init 8/8 → worker.service register
  → procrastinate pickup → gpu_ready SSE → 8 GPU fan-out
  → 100/100 images → completed notify (T7p slim) → done SSE
  → runs.completed_at → idle terminate
```

---

## 4. Post-SUCCESS hardening (parking lot, defer to separate plan)

agent가 §43에서 명시. acceptance blocker 아님, 별건 plan으로:

| ID | Description | Priority | Effort |
|---|---|---|---|
| T7r | `runs.completed_at` direct DB write (NOTIFY-independent finality) | medium | small |
| T7s | systemd worker.service `After=cloud-final.service` (cold-boot env-write race) | medium | small |
| T7t | per-image-results endpoint (full result on disk, fetch by run_id) | low | medium |
| ladder lower-tier | exercise 24xlarge / 12xlarge / 4xlarge live (only tier 1 hit so far) | low | needs drought |
| AMI rebake | refresh AMI to current main (vendor advance + peft pre-baked + bake script `git submodule sync` fix) | low | $0.50 + 30 min |
| 8-GPU multi-GPU `_run_sam_multi_gpu` | currently bypassed by T7l; merged_m3 producer needs vendor-compatible shape | low | algo work |

---

## 5. 다음 세션 진입점

1. 이 doc (`handoff-2026-06-12.md`) 5분
2. `CLAUDE.md` PM 룰 skim 2분
3. `docs/sam-ops.md §43` (SUCCESS writeup) 5분
4. **다음 한 발**:
   - 만약 prod feature로 묶기 원하면 → §4 hardening plan brainstorm
   - 만약 다른 작업 원하면 → 백로그 (W6.1 Cognito / W8 Part 2 SSM / W9 reg_covar / Compute Pipeline 진짜 web UI integration)
5. 또는 web UI 직접 클릭으로 acceptance 재현 (§43은 httpx로 검증 — 같은 path이지만 owner UX 직접 경험 원하면 dev server start)

---

## 6. PM 룰 위반 self-report (이번 세션)

1. **`6090d9c` vendor revert가 잘못된 SHA로 갔음** — git log 잘못 읽어서 `61eb37d`로 revert했지만 진짜 baked SHA는 `2c69ebd`였음. agent의 SSM probe가 catch, T7h로 수정. 추가 시도 비용 ~$0.6.
2. **세션 초반 onion-peel 계속 시도** — §28~§42에서 매번 한 layer fix 후 다음 retry, 다음 layer 노출 패턴이 ~10번 반복. 전체적으로 fix 다 박힌 결과는 좋지만 (cumulative $9.79), 같은 지점 반복 시 큰 fix(예: AMI rebake)로 한 번에 해결할지 owner 더 빨리 알렸어야.
3. **agent가 8장→1장 ladder 같은 architectural 결정을 owner에게 묻기 전에 추천** — 결과적으로 owner directive와 일치했지만, 더 큰 결정은 묻고 가는 게 낫다.

---

## 7. 인프라 핵심 ID

| | |
|---|---|
| AWS profile / region | `qpress` / `us-east-2` |
| RDS endpoint | `qpressdb.ch08y4ooqgmq.us-east-2.rds.amazonaws.com:5432` |
| RDS secret ARN | `arn:aws:secretsmanager:us-east-2:931886963315:secret:rds!db-beb90dd0-feef-45a5-b8b5-81af8d02e0d6-Cwxa1w` |
| Bastion EC2 | `i-063165d449976b2e4` (state: stopped after this session) |
| GPU LT | `qpress-sam-gpu-worker` v37 default |
| GPU AMI | `ami-0b7ec5ff47a1eff11` (qpress-saa-sam-warmup-2026-05-28) |
| GPU IAM role | `qpress-sam-gpu-role` (3 inline policies: s3, secret-rw, SSM core) |
| 3 subnets | us-east-2a `subnet-0fe8558512beea68a` / 2b `subnet-0fe98fb0f6d63afc3` / 2c `subnet-09f76839fd0c109a9` |
| GPU SG | `sg-0e57146d5b6d42452` |
| Vendor private repo | `HoukJangBNL/QPress-SAM-Flake` SHA `2c69ebd` (== AMI baked SHA after T7h) |

---

*PM: Claude (메인 세션). Owner: Houk Jang. 마지막 업데이트: 2026-06-12 EOD.*
